/**
 * `/comfy/api/v1.js` — the published API, as a converted pack imports it.
 *
 * This is the real implementation, not a stub: the point of the harness is to
 * run converted code against the surface it will actually get.
 */
export { comfy } from './comfyInstance.mjs'
