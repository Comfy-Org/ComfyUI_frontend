/**
 * Test harness for v1/v2 extension compatibility tests.
 *
 * Re-exports from ./harness/ for backward compatibility with test imports
 * that use `from '../harness'` instead of `from '../harness/index'`.
 */
export * from './harness/index'
