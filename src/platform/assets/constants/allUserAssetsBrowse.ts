/**
 * Pass as {@link AssetBrowserModal} `assetType` to load merged cloud assets
 * (input, output, models, temp) into the browser cache.
 */
export const ALL_USER_ASSETS_ASSET_TYPE = '__all_user__' as const

export const ALL_USER_ASSETS_CACHE_CATEGORY = 'tag:__all_user__' as const
