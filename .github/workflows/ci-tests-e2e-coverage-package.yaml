name: 'CI: E2E Coverage Package'

on:
  workflow_call:
    inputs:
      source_run_id:
        required: true
        type: string
      source_sha:
        required: true
        type: string

permissions:
  actions: read
  contents: read

jobs:
  package:
    if: github.repository == 'Comfy-Org/ComfyUI_frontend'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout sources for coverage packaging
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.source_sha }}
          sparse-checkout: |
            src
            packages
            scripts/cicd/package-e2e-coverage.sh

      - name: Download all shard coverage data
        uses: dawidd6/action-download-artifact@0bd50d53a6d7fb5cb921e607957e9cc12b4ce392 # v12
        with:
          run_id: ${{ inputs.source_run_id }}
          name: e2e-coverage-shard-.*
          name_is_regexp: true
          path: temp/coverage-shards
          if_no_artifact_found: warn

      - name: Install lcov
        run: sudo apt-get install -y -qq lcov

      - name: Package shard coverage
        id: coverage
        run: |
          scripts/cicd/package-e2e-coverage.sh \
            temp/coverage-shards \
            coverage/playwright \
            coverage/html

      - name: Upload merged coverage data
        if: steps.coverage.outputs.has-coverage == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: e2e-coverage
          path: coverage/playwright/
          retention-days: 30
          if-no-files-found: warn

      - name: Upload HTML report artifact
        if: steps.coverage.outputs.has-coverage == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: e2e-coverage-html
          path: coverage/html/
          retention-days: 30
