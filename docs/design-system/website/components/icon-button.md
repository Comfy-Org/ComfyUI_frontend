---
id: website.icon-button
component: IconButton
implementation: apps/website/src/components/ui/icon-button/IconButton.vue
status: approved
provenance: established-website-code
variants: ghost, outline, solid
sizes: sm, default, lg
class_policy: none
---

# IconButton

Established website control for icon-only actions. Use `as="a"` with an
`href` for navigation and provide an action-specific accessible label. The
`solid` `sm` treatment is the approved family-row action; its arrow uses the
existing `/icons/arrow-right.svg` asset.
