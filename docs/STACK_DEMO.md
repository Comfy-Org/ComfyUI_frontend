# Stacked PR demo (throwaway)

Temporary stack to view GitHub's native stacked-PR UI. Will be deleted.

## Slice 1 — the note

Bottom of the stack. Targets `main`.

## Slice 2 — how it stacks

Targets slice 1's branch. Diff shows only this section.
