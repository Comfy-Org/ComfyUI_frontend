---
interface Props {
  items: string[]
}

const { items } = Astro.props
---

<ol class="mt-4 space-y-1 pl-1 text-sm [counter-reset:step]">
  {
    items.map((item) => (
      <li class="flex items-start gap-3 text-primary-comfy-canvas [counter-increment:step] before:shrink-0 before:font-semibold before:text-primary-comfy-yellow before:tabular-nums before:content-[counter(step,decimal-leading-zero)]">
        {item}
      </li>
    ))
  }
</ol>
