{
  "id": "runway/aleph2-video-to-video",
  "slug": "runway--aleph2-video-to-video",
  "displayName": "Runway Aleph 2",
  "provider": "runway",
  "modality": "video",
  "description": "Edits an input video with a text prompt, preserving motion and timing (Runway Aleph2).",
  "tags": ["runway", "aleph2", "video", "video-to-video"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["runway/aleph2-video-to-video"]
      },
      "prompt": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1000,
        "description": "Describes what should appear in the output (1-1000 characters)."
      },
      "seed": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295,
        "description": "Random seed for generation.",
        "default": 0
      },
      "medias": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["video"]
            },
            "value": {
              "type": "string"
            }
          },
          "required": ["role", "value"],
          "additionalProperties": false
        }
      },
      "public_figure_threshold": {
        "type": "string",
        "enum": ["auto", "low"],
        "description": "Content moderation for recognizable public figures.",
        "default": "low"
      }
    },
    "required": ["model", "prompt"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "video",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    }
  ]
}
