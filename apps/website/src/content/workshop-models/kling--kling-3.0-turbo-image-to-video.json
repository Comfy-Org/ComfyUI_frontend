{
  "id": "kling/kling-3.0-turbo-image-to-video",
  "slug": "kling--kling-3.0-turbo-image-to-video",
  "displayName": "Kling 3.0 Turbo Image-to-Video",
  "provider": "kling",
  "modality": "video",
  "description": "Animates a first-frame image with Kling 3.0 Turbo.",
  "tags": ["kling", "3.0", "turbo", "video", "image-to-video"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["kling/kling-3.0-turbo-image-to-video"]
      },
      "prompt": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2500
      },
      "dispatch_mode": {
        "type": "string",
        "enum": ["sync", "async"],
        "description": "Dispatch mode. \"sync\" (default) blocks until the job completes. \"async\" submits the job and returns immediately with a job ID; poll completion via `job_get`. Named `dispatch_mode` (not bare `mode`) so it never collides with provider-specific `mode` knobs (freepik DynamicCombo, pixverse motion mode, meshy pose mode, …).",
        "default": "sync"
      },
      "medias": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["start_image"]
            },
            "value": {
              "type": "string"
            }
          },
          "required": ["role", "value"],
          "additionalProperties": false
        }
      },
      "settings": {
        "type": "object",
        "properties": {
          "resolution": {
            "type": "string",
            "enum": ["720p", "1080p"],
            "default": "720p"
          },
          "duration": {
            "type": "integer",
            "minimum": 3,
            "maximum": 15,
            "default": 5
          }
        },
        "additionalProperties": false,
        "default": {}
      }
    },
    "required": ["model"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "start_image",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    }
  ]
}
