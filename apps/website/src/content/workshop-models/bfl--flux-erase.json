{
  "id": "bfl/flux-erase",
  "slug": "bfl--flux-erase",
  "displayName": "FLUX Tools Erase",
  "provider": "bfl",
  "modality": "image",
  "description": "Removes the masked object from an image and reconstructs the background (Flux Tools Erase).",
  "tags": [
    "bfl",
    "flux",
    "erase",
    "object-removal",
    "inpaint",
    "edit",
    "image-edit"
  ],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["bfl/flux-erase"]
      },
      "seed": {
        "type": "integer"
      },
      "medias": {
        "type": "array",
        "items": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string",
                  "enum": ["image"]
                },
                "value": {
                  "type": "string"
                }
              },
              "required": ["role", "value"],
              "additionalProperties": false
            },
            {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string",
                  "enum": ["mask"]
                },
                "value": {
                  "type": "string"
                }
              },
              "required": ["role", "value"],
              "additionalProperties": false
            }
          ]
        }
      },
      "dilate_pixels": {
        "type": "integer"
      }
    },
    "required": ["model"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "image",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    },
    {
      "role": "mask",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    }
  ]
}
