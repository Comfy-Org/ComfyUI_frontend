{
  "id": "meshy/texture",
  "slug": "meshy--texture",
  "displayName": "Meshy Texture",
  "provider": "meshy",
  "modality": "3d",
  "description": "Re-textures an existing Meshy 3D model with a new style. Pass `meshy_task_id` plus exactly one of `text_style_prompt` or `image_style`.",
  "tags": ["meshy", "3d", "texture", "retexture", "post-process"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["meshy/texture"]
      },
      "dispatch_mode": {
        "type": "string",
        "enum": ["sync", "async"],
        "description": "Dispatch mode. \"sync\" (default) blocks until the job completes. \"async\" submits the job and returns immediately with a job ID; poll completion via `job_get`. Named `dispatch_mode` (not bare `mode`) so it never collides with provider-specific `mode` knobs (freepik DynamicCombo, pixverse motion mode, meshy pose mode, …).",
        "default": "sync"
      },
      "enable_original_uv": {
        "type": "boolean",
        "description": "Use the original UV of the model instead of generating new UVs.",
        "default": true
      },
      "meshy_task_id": {
        "type": "string",
        "description": "The task ID of a completed 3D model that needs to be re-textured."
      },
      "pbr": {
        "type": "boolean",
        "description": "Generate PBR Maps in addition to the base color (renamed `enable_pbr` on the wire).",
        "default": false
      },
      "text_style_prompt": {
        "type": "string",
        "description": "Describe the desired texture style using text. Maximum 600 characters. Cannot be used at the same time as 'image_style'.",
        "default": ""
      },
      "texture_resolution": {
        "type": "string",
        "enum": ["2k", "4k", "8k"],
        "description": "Base color texture resolution. Higher resolutions capture more surface detail; 8k costs 15 credits instead of 10.",
        "default": "2k"
      }
    },
    "required": ["model", "meshy_task_id"],
    "additionalProperties": true
  },
  "roles": []
}
