{
  "id": "vidu/reference-video-q2",
  "slug": "vidu--reference-video-q2",
  "displayName": "Vidu Q2 Reference-to-Video",
  "provider": "vidu",
  "modality": "video",
  "description": "Generates a video from up to seven reference images with optional dialogue and music (Vidu Q2).",
  "tags": ["vidu", "viduq2", "video", "reference-to-video"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["vidu/reference-video-q2"]
      },
      "prompt": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "description": "A textual description for video generation."
      },
      "aspect_ratio": {
        "type": "string",
        "enum": ["16:9", "9:16", "4:3", "3:4", "1:1"],
        "description": "The aspect ratio of the output video.",
        "default": "16:9"
      },
      "duration": {
        "type": "integer",
        "minimum": 1,
        "maximum": 10,
        "description": "Duration of the output video in seconds.",
        "default": 5
      },
      "seed": {
        "type": "integer",
        "minimum": 0,
        "maximum": 2147483647,
        "description": "Seed for video generation (0 for random).",
        "default": 1
      },
      "dispatch_mode": {
        "type": "string",
        "enum": ["sync", "async"],
        "description": "Dispatch mode. \"sync\" (default) blocks until the job completes. \"async\" submits the job and returns immediately with a job ID; poll completion via `job_get`. Named `dispatch_mode` (not bare `mode`) so it never collides with provider-specific `mode` knobs (freepik DynamicCombo, pixverse motion mode, meshy pose mode, …).",
        "default": "sync"
      },
      "medias": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["image"]
            },
            "value": {
              "type": "string"
            }
          },
          "required": ["role", "value"],
          "additionalProperties": false
        }
      },
      "audio": {
        "type": "boolean",
        "description": "When enabled video will contain generated speech and background music based on the prompt.",
        "default": false
      },
      "movement_amplitude": {
        "type": "string",
        "enum": ["auto", "small", "medium", "large"],
        "description": "The movement amplitude of objects in the frame."
      },
      "resolution": {
        "type": "string",
        "enum": ["720p", "1080p"],
        "description": "Resolution of the output video."
      }
    },
    "required": ["model", "prompt", "movement_amplitude", "resolution"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "image",
      "required": true,
      "cardinality": "many",
      "minItems": 1,
      "maxItems": 7
    }
  ]
}
