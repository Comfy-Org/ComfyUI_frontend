{
  "id": "bfl/flux-virtual-try-on",
  "slug": "bfl--flux-virtual-try-on",
  "displayName": "FLUX Virtual Try-On",
  "provider": "bfl",
  "modality": "image",
  "description": "Virtual try-on: dresses a person image in a provided garment image (Flux Tools VTO).",
  "tags": ["bfl", "flux", "virtual-try-on", "vto", "fashion", "edit"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["bfl/flux-virtual-try-on"]
      },
      "prompt": {
        "type": "string"
      },
      "seed": {
        "type": "integer"
      },
      "medias": {
        "type": "array",
        "items": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string",
                  "enum": ["garment_image"]
                },
                "value": {
                  "type": "string"
                }
              },
              "required": ["role", "value"],
              "additionalProperties": false
            },
            {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string",
                  "enum": ["model_image"]
                },
                "value": {
                  "type": "string"
                }
              },
              "required": ["role", "value"],
              "additionalProperties": false
            }
          ]
        }
      }
    },
    "required": ["model"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "garment_image",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    },
    {
      "role": "model_image",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    }
  ]
}
