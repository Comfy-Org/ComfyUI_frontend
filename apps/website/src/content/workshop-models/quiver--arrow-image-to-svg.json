{
  "id": "quiver/arrow-image-to-svg",
  "slug": "quiver--arrow-image-to-svg",
  "displayName": "Quiver Arrow Image-to-SVG",
  "provider": "quiver",
  "modality": "svg",
  "description": "Vectorises a raster image into an SVG (Quiver Arrow).",
  "tags": ["quiver", "arrow", "svg", "vector", "image-to-svg", "vectorize"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["quiver/arrow-image-to-svg"]
      },
      "medias": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["image"]
            },
            "value": {
              "type": "string"
            }
          },
          "required": ["role", "value"],
          "additionalProperties": false
        }
      },
      "auto_crop": {
        "type": "boolean",
        "default": false
      },
      "presence_penalty": {
        "type": "number",
        "minimum": -2,
        "maximum": 2,
        "default": 0
      },
      "target_size": {
        "type": "integer",
        "minimum": 128,
        "maximum": 4096,
        "default": 1024
      },
      "temperature": {
        "type": "number",
        "minimum": 0,
        "maximum": 2,
        "default": 1
      },
      "top_p": {
        "type": "number",
        "minimum": 0.05,
        "maximum": 1,
        "default": 1
      }
    },
    "required": ["model"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "image",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    }
  ]
}
