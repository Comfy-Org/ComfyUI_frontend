{
  "id": "openai/dall-e-3",
  "slug": "openai--dall-e-3",
  "displayName": "DALL·E 3",
  "provider": "openai",
  "modality": "image",
  "description": "Generates an image from text with selectable quality and style (DALL·E 3).",
  "tags": ["openai", "dalle", "dall-e-3", "text-to-image"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["openai/dall-e-3"]
      },
      "prompt": {
        "type": "string",
        "minLength": 1,
        "description": "Text prompt for DALL·E (node.py:254-257)."
      },
      "quality": {
        "type": "string",
        "enum": ["standard", "hd"],
        "description": "Image quality (node.py:271-274).",
        "default": "standard"
      },
      "size": {
        "type": "string",
        "enum": ["1024x1024", "1024x1792", "1792x1024"],
        "description": "Image size (node.py:285-288).",
        "default": "1024x1024"
      },
      "style": {
        "type": "string",
        "enum": ["natural", "vivid"],
        "description": "Image style (node.py:278-281).",
        "default": "natural"
      }
    },
    "required": ["model", "prompt"],
    "additionalProperties": true
  },
  "roles": []
}
