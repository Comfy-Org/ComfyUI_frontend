{
  "id": "bria/green-screen-video",
  "slug": "bria--green-screen-video",
  "displayName": "Bria Green Screen Video",
  "provider": "bria",
  "modality": "video",
  "description": "Replaces a video's background with a solid chroma-key screen (Bria).",
  "tags": ["bria", "green-screen", "chroma-key", "video-edit"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["bria/green-screen-video"]
      },
      "seed": {
        "type": "integer",
        "minimum": 0,
        "maximum": 2147483647,
        "description": "Seed controls whether the node should re-run; results are non-deterministic regardless of seed.",
        "default": 0
      },
      "medias": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["video"]
            },
            "value": {
              "type": "string"
            }
          },
          "required": ["role", "value"],
          "additionalProperties": false
        }
      },
      "green_shade": {
        "type": "string",
        "enum": ["broadcast_green", "chroma_green", "blue_screen"],
        "description": "Solid chroma-key shade applied behind the foreground: broadcast_green (#00B140), chroma_green (#00FF00), or blue_screen (#0000FF)."
      }
    },
    "required": ["model", "green_shade"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "video",
      "required": true,
      "cardinality": "single",
      "minItems": 1,
      "maxItems": 1
    }
  ]
}
