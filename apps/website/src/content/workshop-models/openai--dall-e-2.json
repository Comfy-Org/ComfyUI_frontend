{
  "id": "openai/dall-e-2",
  "slug": "openai--dall-e-2",
  "displayName": "DALL·E 2",
  "provider": "openai",
  "modality": "image",
  "description": "Generates an image from text or inpaints an image when image and mask are provided (DALL·E 2).",
  "tags": ["openai", "dalle", "dall-e-2", "text-to-image", "inpainting"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["openai/dall-e-2"]
      },
      "prompt": {
        "type": "string",
        "minLength": 1
      },
      "count": {
        "type": "integer",
        "minimum": 1,
        "maximum": 8
      },
      "medias": {
        "type": "array",
        "items": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string",
                  "enum": ["image"]
                },
                "value": {
                  "type": "string"
                }
              },
              "required": ["role", "value"],
              "additionalProperties": false
            },
            {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string",
                  "enum": ["mask"]
                },
                "value": {
                  "type": "string"
                }
              },
              "required": ["role", "value"],
              "additionalProperties": false
            }
          ]
        }
      },
      "size": {
        "type": "string",
        "enum": ["256x256", "512x512", "1024x1024"]
      }
    },
    "required": ["model", "prompt"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "image",
      "required": false,
      "cardinality": "single",
      "minItems": 0,
      "maxItems": 1
    },
    {
      "role": "mask",
      "required": false,
      "cardinality": "single",
      "minItems": 0,
      "maxItems": 1
    }
  ]
}
