{
  "id": "pika/scenes",
  "slug": "pika--scenes",
  "displayName": "Pika 2.2 Pikascenes",
  "provider": "pika",
  "modality": "video",
  "description": "Composites multiple ingredient images into one video, blending loosely or preserving spatial layout (Pika 2.2 Pikascenes).",
  "tags": ["pika", "video", "scenes", "compositing"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["pika/scenes"]
      },
      "prompt": {
        "type": "string",
        "minLength": 1
      },
      "duration": {
        "type": "integer",
        "minimum": 0,
        "default": 5
      },
      "seed": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "dispatch_mode": {
        "type": "string",
        "enum": ["sync", "async"],
        "description": "Dispatch mode. \"sync\" (default) blocks until the job completes. \"async\" submits the job and returns immediately with a job ID; poll completion via `job_get`. Named `dispatch_mode` (not bare `mode`) so it never collides with provider-specific `mode` knobs (freepik DynamicCombo, pixverse motion mode, meshy pose mode, …).",
        "default": "sync"
      },
      "medias": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["image"]
            },
            "value": {
              "type": "string"
            }
          },
          "required": ["role", "value"],
          "additionalProperties": false
        }
      },
      "ingredients_mode": {
        "type": "string",
        "enum": ["creative", "precise"]
      },
      "negative_prompt": {
        "type": "string"
      },
      "resolution": {
        "type": "string",
        "default": "1080p"
      }
    },
    "required": ["model", "ingredients_mode"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "image",
      "required": true,
      "cardinality": "many",
      "minItems": 1,
      "maxItems": 8
    }
  ]
}
