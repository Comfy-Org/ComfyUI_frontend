{
  "id": "sonilo/text-to-music",
  "slug": "sonilo--text-to-music",
  "displayName": "Sonilo Text-to-Music",
  "provider": "sonilo",
  "modality": "music",
  "description": "Generates music from a text prompt with a required duration from 1 to 360 seconds.",
  "tags": ["sonilo", "music", "text-to-music", "audio"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["sonilo/text-to-music"]
      },
      "prompt": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1000,
        "description": "Text prompt describing the music to generate."
      },
      "duration": {
        "type": "integer",
        "minimum": 1,
        "maximum": 360,
        "description": "Target duration in seconds (1–360)."
      }
    },
    "required": ["model", "prompt", "duration"],
    "additionalProperties": true
  },
  "roles": []
}
