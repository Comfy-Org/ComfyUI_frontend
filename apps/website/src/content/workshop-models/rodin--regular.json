{
  "id": "rodin/regular",
  "slug": "rodin--regular",
  "displayName": "Hyper3D Rodin Regular",
  "provider": "rodin",
  "modality": "3d",
  "description": "Generates a 3D mesh from 1-5 reference images (Hyper3D Rodin Regular).",
  "tags": ["rodin", "hyper3d", "deemos", "3d", "image-to-3d", "regular"],
  "parameters": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["rodin/regular"]
      },
      "medias": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["image"]
            },
            "value": {
              "type": "string"
            }
          },
          "required": ["role", "value"],
          "additionalProperties": false
        }
      },
      "Material_Type": {
        "type": "string",
        "enum": ["PBR", "Shaded"]
      },
      "Polygon_count": {
        "type": "string",
        "enum": ["4K-Quad", "8K-Quad", "18K-Quad", "50K-Quad", "200K-Triangle"]
      },
      "Seed": {
        "type": "integer",
        "minimum": 0,
        "maximum": 65535
      }
    },
    "required": ["model"],
    "additionalProperties": true
  },
  "roles": [
    {
      "role": "image",
      "required": true,
      "cardinality": "many",
      "minItems": 1,
      "maxItems": 5
    }
  ]
}
