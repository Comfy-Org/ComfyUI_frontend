[
{"id":"anthropic/claude-fable-5","catalogId":"anthropic/claude-fable-5","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-fable-5-1","catalogId":"anthropic/claude-fable-5-1","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-haiku-4-5-20251001","catalogId":"anthropic/claude-haiku-4-5-20251001","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-opus-4-6","catalogId":"anthropic/claude-opus-4-6","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-opus-4-7","catalogId":"anthropic/claude-opus-4-7","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-opus-4-8","catalogId":"anthropic/claude-opus-4-8","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-opus-5","catalogId":"anthropic/claude-opus-5","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-sonnet-4-5-20250929","catalogId":"anthropic/claude-sonnet-4-5-20250929","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-sonnet-4-6","catalogId":"anthropic/claude-sonnet-4-6","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"anthropic/claude-sonnet-5","catalogId":"anthropic/claude-sonnet-5","description":"Request body for Anthropic Messages API (`/v1/messages`). Mirrors the upstream schema with strict typing only on fields the proxy reads (model, prompt extraction, streaming, billing). Other top-level fields (`tools`, `temperature`, `top_p`, `thinking`, `metadata`, etc.) pass through unchanged via `additionalProperties`."},
{"id":"beeble/switchx","catalogId":"beeble/switchx","description":"Request to create and start a SwitchX generation job."},
{"id":"bfl/erase-v1","catalogId":"bfl/erase-v1","description":"Request body for the BFL Flux Tools Erase v1 object removal API."},
{"id":"bfl/flux-2-max","catalogId":"bfl/flux-2-max","description":"Request body for the BFL Flux 2 image generation API."},
{"id":"bfl/flux-2-pro","catalogId":"bfl/flux-2-pro","description":"Request body for the BFL Flux 2 image generation API."},
{"id":"bfl/flux-3-video","catalogId":"bfl/flux-3-video","description":"Request body for BFL FLUX 3 video generation. The mode field selects the generation type and decides which fields are required: t2v needs prompt; i2v needs prompt and keyframes; v2v needs prompt and start_video; draft_enhance needs draft_cache and accepts no other generation parameters."},
{"id":"bfl/flux-kontext-max","catalogId":"bfl/flux-kontext-max","description":"Request body for the BFL FLUX.1 Kontext [max] API. Edits input_image when one is supplied; generates from the prompt alone when it is not."},
{"id":"bfl/flux-kontext-pro","catalogId":"bfl/flux-kontext-pro","description":"Request body for the BFL FLUX.1 Kontext [pro] API. Edits input_image when one is supplied; generates from the prompt alone when it is not."},
{"id":"bfl/flux-pro-1.0-canny","catalogId":"bfl/flux-pro-1.0-canny"},
{"id":"bfl/flux-pro-1.0-depth","catalogId":"bfl/flux-pro-1.0-depth"},
{"id":"bfl/flux-pro-1.0-expand","catalogId":"bfl/flux-pro-1.0-expand"},
{"id":"bfl/flux-pro-1.0-fill","catalogId":"bfl/flux-pro-1.0-fill"},
{"id":"bfl/flux-pro-1.1","catalogId":"bfl/flux-pro-1.1","description":"Request body for the BFL FLUX 1.1 [pro] image generation API."},
{"id":"bfl/flux-pro-1.1-ultra","catalogId":"bfl/flux-pro-1.1-ultra","description":"Request body for the BFL FLUX 1.1 [pro] Ultra image generation API. Ultra selects the output size from aspect_ratio rather than explicit pixel dimensions."},
{"id":"bfl/video-upscale-v1","catalogId":"bfl/video-upscale-v1","description":"Request body for the BFL Flux Tools Video Upscale v1 API. Charges are based on the delivered output only; input resolution and upscale factor are not billed separately."},
{"id":"bfl/vto-v1","catalogId":"bfl/vto-v1","description":"Request body for the BFL Flux Tools VTO v1 virtual try-on API."},
{"id":"bria/fibo","catalogId":"bria/fibo","description":"The Bria FIBO edit fields, shared by the v1 `POST /proxy/bria/v2/image/edit` request body and the Comfy Router input schema for `bria/fibo`. See the note above this component for why the two surfaces share properties but not `model_version`'s constraints."},
{"id":"bria/image-edit-erase","catalogId":"bria/image-edit-erase","description":"Request body for Bria Eraser API"},
{"id":"bria/image-edit-expand","catalogId":"bria/image-edit-expand","description":"Request body for Bria Image Expansion API"},
{"id":"bria/image-edit-gen-fill","catalogId":"bria/image-edit-gen-fill","description":"Request body for Bria Generative Fill API"},
{"id":"bria/image-edit-increase-resolution","catalogId":"bria/image-edit-increase-resolution","description":"Request body for Bria Increase Resolution API"},
{"id":"bria/image-edit-remove-background","catalogId":"bria/image-edit-remove-background","description":"Request body for Bria Image Remove Background API"},
{"id":"bria/structured-instruction","catalogId":"bria/structured-instruction","description":"Request body for Bria Structured Instruction Generate API"},
{"id":"bria/video-edit-green-screen","catalogId":"bria/video-edit-green-screen","description":"Request body for Bria Video Green Screen API"},
{"id":"bria/video-edit-remove-background","catalogId":"bria/video-edit-remove-background","description":"Request body for Bria Video Remove Background API"},
{"id":"bria/video-edit-replace-background","catalogId":"bria/video-edit-replace-background","description":"Request body for Bria Video Replace Background API"},
{"id":"byteplus/dreamina-seedance-2-0-260128","catalogId":"byteplus/dreamina-seedance-2-0-260128","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/dreamina-seedance-2-0-fast-260128","catalogId":"byteplus/dreamina-seedance-2-0-fast-260128","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/dreamina-seedance-2-0-mini","catalogId":"byteplus/dreamina-seedance-2-0-mini","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/dreamina-seedance-2-5-260628","catalogId":"byteplus/dreamina-seedance-2-5-260628","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/seed-2-0-lite-260228","catalogId":"byteplus/seed-2-0-lite-260228","description":"Request body Comfy Router accepts for `byteplus/seed-2-0-lite-260228` on POST /v2/models/byteplus/seed-2-0-lite-260228, and the schema `routervalidate.Guard` validates the call against before it reaches BytePlus. It is the Seed 2.0 LITE tier.\nIt composes BytePlusResponseInputs, which is the same field set the v1 `POST /proxy/byteplus/api/v3/responses` request body composes, so the two surfaces cannot drift into two shapes. They differ in `required` alone: this one omits `model`, because Router fills it from the `{model}` path segment after Guard has run (BE-11167), while the v1 surface has no path segment supplying it and keeps requiring it.\n`reasoning.encrypted_content` in `include` is documented by BytePlus as supported from seed-2-0-pro-260328; Comfy does not enforce that here, so the field is accepted on every one of the three and BytePlus remains the authority on which model honours it."},
{"id":"byteplus/seed-2-0-mini-260215","catalogId":"byteplus/seed-2-0-mini-260215","description":"Request body Comfy Router accepts for `byteplus/seed-2-0-mini-260215` on POST /v2/models/byteplus/seed-2-0-mini-260215, and the schema `routervalidate.Guard` validates the call against before it reaches BytePlus. It is the Seed 2.0 MINI tier.\nIt composes BytePlusResponseInputs, which is the same field set the v1 `POST /proxy/byteplus/api/v3/responses` request body composes, so the two surfaces cannot drift into two shapes. They differ in `required` alone: this one omits `model`, because Router fills it from the `{model}` path segment after Guard has run (BE-11167), while the v1 surface has no path segment supplying it and keeps requiring it.\n`reasoning.encrypted_content` in `include` is documented by BytePlus as supported from seed-2-0-pro-260328; Comfy does not enforce that here, so the field is accepted on every one of the three and BytePlus remains the authority on which model honours it."},
{"id":"byteplus/seed-2-0-pro-260328","catalogId":"byteplus/seed-2-0-pro-260328","description":"Request body Comfy Router accepts for `byteplus/seed-2-0-pro-260328` on POST /v2/models/byteplus/seed-2-0-pro-260328, and the schema `routervalidate.Guard` validates the call against before it reaches BytePlus. It is the Seed 2.0 PRO tier.\nIt composes BytePlusResponseInputs, which is the same field set the v1 `POST /proxy/byteplus/api/v3/responses` request body composes, so the two surfaces cannot drift into two shapes. They differ in `required` alone: this one omits `model`, because Router fills it from the `{model}` path segment after Guard has run (BE-11167), while the v1 surface has no path segment supplying it and keeps requiring it.\n`reasoning.encrypted_content` in `include` is documented by BytePlus as supported from seed-2-0-pro-260328; Comfy does not enforce that here, so the field is accepted on every one of the three and BytePlus remains the authority on which model honours it."},
{"id":"byteplus/seed-audio-1.0","catalogId":"byteplus/seed-audio-1.0","description":"Request body for a BytePlus Seed Audio 1.0 generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/tts/create must add `model` to it. One example cannot serve both surfaces — this component is the Router input schema for BOTH seed-audio-1.0 spellings, so an example naming either one is refused by the other model's route for naming a different model, while the omitting form is valid on both."},
{"id":"byteplus/seed-audio-1.0-multilingual","catalogId":"byteplus/seed-audio-1.0-multilingual","description":"Request body for a BytePlus Seed Audio 1.0 generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/tts/create must add `model` to it. One example cannot serve both surfaces — this component is the Router input schema for BOTH seed-audio-1.0 spellings, so an example naming either one is refused by the other model's route for naming a different model, while the omitting form is valid on both."},
{"id":"byteplus/seedance-1-0-lite-i2v-250428","catalogId":"byteplus/seedance-1-0-lite-i2v-250428","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/seedance-1-0-lite-t2v-250428","catalogId":"byteplus/seedance-1-0-lite-t2v-250428","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/seedance-1-0-pro-250528","catalogId":"byteplus/seedance-1-0-pro-250528","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/seedance-1-0-pro-fast-251015","catalogId":"byteplus/seedance-1-0-pro-fast-251015","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/seedance-1-5-pro-251215","catalogId":"byteplus/seedance-1-5-pro-251215","description":"Request body for a BytePlus Seedance / Dreamina video generation task. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/contents/generations/tasks must add `model` to it. This text-to-video example requires an additional `content` item with `type: image_url` and an `image_url.url` for seedance-1-0-lite-i2v-250428. Text-only models must omit that image item.\n`content` is the whole `required` floor, and it is the floor the v1 surface already published and videoGenerationProxy's Rewrite already refuses an empty value for. Everything else BytePlus accepts varies by model — the per-model resolutions, durations, ratios and input kinds are documented on the individual fields — so the enums and bounds below are the union BytePlus admits across the family and BytePlus stays the authority on which member accepts which value."},
{"id":"byteplus/seededit-3-0-i2i-250628","catalogId":"byteplus/seededit-3-0-i2i-250628","description":"Request body for a BytePlus Seedream / Seededit image generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/images/generations must add `model` to it. The example illustrates text-to-image calls. For seededit-3-0-i2i-250628, also supply the single `image` URL or base64 image to edit; omit `size` to retain that model's adaptive sizing. Layer decomposition accepts `image` without `prompt`. At least one of `prompt` or `image` is required; BytePlus validates the model-specific combination. The v1 wrapper separately requires `model`."},
{"id":"byteplus/seedream-3-0-t2i-250415","catalogId":"byteplus/seedream-3-0-t2i-250415","description":"Request body for a BytePlus Seedream / Seededit image generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/images/generations must add `model` to it. The example illustrates text-to-image calls. For seededit-3-0-i2i-250628, also supply the single `image` URL or base64 image to edit; omit `size` to retain that model's adaptive sizing. Layer decomposition accepts `image` without `prompt`. At least one of `prompt` or `image` is required; BytePlus validates the model-specific combination. The v1 wrapper separately requires `model`."},
{"id":"byteplus/seedream-4-0-250828","catalogId":"byteplus/seedream-4-0-250828","description":"Request body for a BytePlus Seedream / Seededit image generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/images/generations must add `model` to it. The example illustrates text-to-image calls. For seededit-3-0-i2i-250628, also supply the single `image` URL or base64 image to edit; omit `size` to retain that model's adaptive sizing. Layer decomposition accepts `image` without `prompt`. At least one of `prompt` or `image` is required; BytePlus validates the model-specific combination. The v1 wrapper separately requires `model`."},
{"id":"byteplus/seedream-4-5-251128","catalogId":"byteplus/seedream-4-5-251128","description":"Request body for a BytePlus Seedream / Seededit image generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/images/generations must add `model` to it. The example illustrates text-to-image calls. For seededit-3-0-i2i-250628, also supply the single `image` URL or base64 image to edit; omit `size` to retain that model's adaptive sizing. Layer decomposition accepts `image` without `prompt`. At least one of `prompt` or `image` is required; BytePlus validates the model-specific combination. The v1 wrapper separately requires `model`."},
{"id":"byteplus/seedream-5-0-260128","catalogId":"byteplus/seedream-5-0-260128","description":"Request body for a BytePlus Seedream / Seededit image generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/images/generations must add `model` to it. The example illustrates text-to-image calls. For seededit-3-0-i2i-250628, also supply the single `image` URL or base64 image to edit; omit `size` to retain that model's adaptive sizing. Layer decomposition accepts `image` without `prompt`. At least one of `prompt` or `image` is required; BytePlus validates the model-specific combination. The v1 wrapper separately requires `model`."},
{"id":"byteplus/seedream-5-0-pro-260628","catalogId":"byteplus/seedream-5-0-pro-260628","description":"Request body for a BytePlus Seedream / Seededit image generation. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/byteplus/api/v3/images/generations must add `model` to it. The example illustrates text-to-image calls. For seededit-3-0-i2i-250628, also supply the single `image` URL or base64 image to edit; omit `size` to retain that model's adaptive sizing. Layer decomposition accepts `image` without `prompt`. At least one of `prompt` or `image` is required; BytePlus validates the model-specific combination. The v1 wrapper separately requires `model`."},
{"id":"elevenlabs/eleven_sfx_v2","catalogId":"elevenlabs/eleven_sfx_v2","description":"Request body for generating sound effects from text. The `example` below is the COMFY ROUTER form and omits `model_id`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/elevenlabs/v1/sound-generation may send it, and the route admits `eleven_sfx_v2` alone."},
{"id":"elevenlabs/eleven_v3","catalogId":"elevenlabs/eleven_v3","description":"Request body for ElevenLabs Text-to-Dialogue (multi-voice TTS). The `example` below is the COMFY ROUTER form and omits `model_id`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/elevenlabs/v1/text-to-dialogue may send it, and the route admits `eleven_v3` alone."},
{"id":"fal/h3-max","catalogId":"fal/h3-max","description":"Request body for the MiniMax H3 Max text-to-video model (minimax/h3-max/text-to-video): generate a video from a text prompt."},
{"id":"fal/h3-max-turbo","catalogId":"fal/h3-max-turbo","description":"Request body for the MiniMax H3 Max Turbo text-to-video model (minimax/h3-max-turbo/text-to-video): generate a video from a text prompt."},
{"id":"fal/patina","catalogId":"fal/patina","description":"Request body for the fal PATINA image-to-image model (fal-ai/patina): predict PBR maps from a single input image."},
{"id":"freepik/ai-image-upscaler-precision-v2","catalogId":"freepik/ai-image-upscaler-precision-v2"},
{"id":"freepik/ai-skin-enhancer-creative","catalogId":"freepik/ai-skin-enhancer-creative"},
{"id":"freepik/ai-skin-enhancer-faithful","catalogId":"freepik/ai-skin-enhancer-faithful"},
{"id":"freepik/ai-skin-enhancer-flexible","catalogId":"freepik/ai-skin-enhancer-flexible"},
{"id":"gemini-interactions/gemini-omni-1.1-flash","catalogId":"gemini-interactions/gemini-omni-1.1-flash","description":"The Gemini Interactions fields, shared by the v1 `POST /proxy/gemini-interactions` request body and the Comfy Router input schema for the `gemini-interactions/*` models. Mirrors the upstream `/v1beta/interactions` schema with strict typing only on the fields the proxy reads (model, `input` for prompt extraction); all other fields (`response_format`, `generation_config`, `store`, `safety_settings`, ...) pass through unchanged via `additionalProperties`. TWO EXCEPTIONS, refused rather than forwarded: `background` and `stream`. Both are honoured by Google and both produce a 2xx this proxy cannot meter — `background: true` answers a queued `in_progress` document carrying no `usage`, whose finished generation is only ever reachable through `GET /proxy/gemini-interactions/{id}`, which meters nothing; `stream: true` answers a `text/event-stream` the billing path cannot decode. Naming either with a value other than `false` or `null` is a 400; omitting them, or sending them false/null, is the synchronous request this surface serves. On the Comfy Router route `POST /v2/models/gemini-interactions/{model}` both fields are settled to `false` before dispatch (routerSettledBoolFields) rather than refused. `required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes; the v1 wrapper `GeminiInteractionRequest` re-adds it."},
{"id":"gemini-interactions/gemini-omni-flash-preview","catalogId":"gemini-interactions/gemini-omni-flash-preview","description":"The Gemini Interactions fields, shared by the v1 `POST /proxy/gemini-interactions` request body and the Comfy Router input schema for the `gemini-interactions/*` models. Mirrors the upstream `/v1beta/interactions` schema with strict typing only on the fields the proxy reads (model, `input` for prompt extraction); all other fields (`response_format`, `generation_config`, `store`, `safety_settings`, ...) pass through unchanged via `additionalProperties`. TWO EXCEPTIONS, refused rather than forwarded: `background` and `stream`. Both are honoured by Google and both produce a 2xx this proxy cannot meter — `background: true` answers a queued `in_progress` document carrying no `usage`, whose finished generation is only ever reachable through `GET /proxy/gemini-interactions/{id}`, which meters nothing; `stream: true` answers a `text/event-stream` the billing path cannot decode. Naming either with a value other than `false` or `null` is a 400; omitting them, or sending them false/null, is the synchronous request this surface serves. On the Comfy Router route `POST /v2/models/gemini-interactions/{model}` both fields are settled to `false` before dispatch (routerSettledBoolFields) rather than refused. `required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes; the v1 wrapper `GeminiInteractionRequest` re-adds it."},
{"id":"heygen/starfish","catalogId":"heygen/starfish","description":"Request body for HeyGen v3 text-to-speech generation"},
{"id":"ideogram/ideogram-v3","catalogId":"ideogram/ideogram-v3","description":"The Ideogram 3.0 (V3) text-to-image fields Comfy Router accepts for `ideogram/ideogram-v3`, and the schema `routervalidate.Guard` validates a call against before it reaches Ideogram.\nIt composes `IdeogramV3Fields`, the same field set the v1 `POST /proxy/ideogram/ideogram-v3/generate` request body composes, so the two surfaces cannot drift into two shapes. It differs from the v1 surface by REFUSING the three reference-image members: they are file uploads and Comfy Router dispatches `application/json` only, so no Router call can supply them. Use the v1 `/proxy/` route with a `multipart/form-data` body for style or character references."},
{"id":"ideogram/ideogram-v4","catalogId":"ideogram/ideogram-v4","description":"Parameters for the Ideogram 4.0 (V4) text-to-image generation proxy request. Supply exactly one of text_prompt or json_prompt."},
{"id":"ideogram/p-image-ideogram","catalogId":"ideogram/p-image-ideogram","description":"Parameters for the P-Image-Ideogram text-to-image generation proxy request. The v1 `/proxy/` route reads these as multipart/form-data, which is also why Comfy Router cannot invoke this model yet: Router dispatches application/json only and refuses the call 403 not_enabled before validation. Use the v1 route until that changes.","unavailableReason":"router-not-enabled"},
{"id":"kling/kling-3.0-turbo","catalogId":"kling/kling-3.0-turbo","description":"Request body for a Kling 3.0 Turbo text-to-video generation. `prompt` is the one field Kling requires. The model is named by the route on both surfaces -- the v1 path ends in the model spelling and the Comfy Router path is /v2/models/kling/kling-3.0-turbo -- so the body carries no model field at all and the same `required` list serves both."},
{"id":"kling/kling-image-o1","catalogId":"kling/kling-image-o1","description":"Request body for a Kling omni-image generation. `prompt` is the one field Kling requires and it stays on this Router-safe floor -- the v1 component has required it since the operation was added, and `prompt` is not a field Router fills, so requiring it costs a Router caller nothing. The `example` below is the COMFY ROUTER form and omits `model_name`, which Router fills from the `{model}` path segment."},
{"id":"kling/kling-v1","catalogId":"kling/kling-v1","description":"Request body for a Kling text-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/text2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both surfaces -- this component is the Router input schema for all seven spellings, so an example naming one of them is refused by the other six routes for naming a different model, while the omitting form is valid on all seven."},
{"id":"kling/kling-v1-5","catalogId":"kling/kling-v1-5","description":"Request body for a Kling image-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/image2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both spellings, so the omitting form is the only one valid on both Router routes."},
{"id":"kling/kling-v1-6","catalogId":"kling/kling-v1-6","description":"Request body for a Kling text-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/text2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both surfaces -- this component is the Router input schema for all seven spellings, so an example naming one of them is refused by the other six routes for naming a different model, while the omitting form is valid on all seven."},
{"id":"kling/kling-v2-1","catalogId":"kling/kling-v2-1","description":"Request body for a Kling image-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/image2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both spellings, so the omitting form is the only one valid on both Router routes."},
{"id":"kling/kling-v2-1-master","catalogId":"kling/kling-v2-1-master","description":"Request body for a Kling text-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/text2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both surfaces -- this component is the Router input schema for all seven spellings, so an example naming one of them is refused by the other six routes for naming a different model, while the omitting form is valid on all seven."},
{"id":"kling/kling-v2-5-turbo","catalogId":"kling/kling-v2-5-turbo","description":"Request body for a Kling text-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/text2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both surfaces -- this component is the Router input schema for all seven spellings, so an example naming one of them is refused by the other six routes for naming a different model, while the omitting form is valid on all seven."},
{"id":"kling/kling-v2-6","catalogId":"kling/kling-v2-6","description":"Request body for a Kling text-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/text2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both surfaces -- this component is the Router input schema for all seven spellings, so an example naming one of them is refused by the other six routes for naming a different model, while the omitting form is valid on all seven."},
{"id":"kling/kling-v2-master","catalogId":"kling/kling-v2-master","description":"Request body for a Kling text-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/text2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both surfaces -- this component is the Router input schema for all seven spellings, so an example naming one of them is refused by the other six routes for naming a different model, while the omitting form is valid on all seven."},
{"id":"kling/kling-v3","catalogId":"kling/kling-v3","description":"Request body for a Kling text-to-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/text2video may add it (the proxy defaults an absent one to kling-v1). One example cannot serve both surfaces -- this component is the Router input schema for all seven spellings, so an example naming one of them is refused by the other six routes for naming a different model, while the omitting form is valid on all seven."},
{"id":"kling/kling-v3-omni","catalogId":"kling/kling-v3-omni","description":"Request body for a Kling omni-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/omni-video may add it (the proxy defaults an absent one to kling-video-o1). One example cannot serve both spellings, so the omitting form is the only one valid on both Router routes."},
{"id":"kling/kling-video-o1","catalogId":"kling/kling-video-o1","description":"Request body for a Kling omni-video generation. The `example` below is the COMFY ROUTER form and deliberately omits `model_name`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/kling/v1/videos/omni-video may add it (the proxy defaults an absent one to kling-video-o1). One example cannot serve both spellings, so the omitting form is the only one valid on both Router routes."},
{"id":"kling/videos-avatar-image2video","catalogId":"kling/videos-avatar-image2video","description":"Generate an avatar video from an image and exactly one audio source: audio_id or sound_file."},
{"id":"kling/videos-lip-sync","catalogId":"kling/videos-lip-sync","description":"Generate lip-sync for a video_id or video_url. Use text2video with text and voice_id, or audio2video with an audio_file or audio_url."},
{"id":"kling/videos-video-extend","catalogId":"kling/videos-video-extend","description":"Extend the video identified by video_id. An explicit video is required; the shared provider account cannot select a caller-specific recent video."},
{"id":"krea/krea-2","catalogId":"krea/krea-2"},
{"id":"krea/krea-2-large","catalogId":"krea/krea-2-large"},
{"id":"krea/krea-2-medium","catalogId":"krea/krea-2-medium"},
{"id":"krea/krea-2-medium-turbo","catalogId":"krea/krea-2-medium-turbo"},
{"id":"ltx/ltx-2-5-fast","catalogId":"ltx/ltx-2-5-fast","description":"The LTX text-to-video fields, shared by the v1 `POST /proxy/ltx/v1/text-to-video` and `POST /proxy/ltx/v2/text-to-video` request bodies and the Comfy Router input schema for the `ltx/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `LTXText2VideoRequest` re-adds it."},
{"id":"ltx/ltx-2-5-pro","catalogId":"ltx/ltx-2-5-pro","description":"The LTX text-to-video fields, shared by the v1 `POST /proxy/ltx/v1/text-to-video` and `POST /proxy/ltx/v2/text-to-video` request bodies and the Comfy Router input schema for the `ltx/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `LTXText2VideoRequest` re-adds it."},
{"id":"luma/photon-1","catalogId":"luma/photon-1","description":"The image generation request object"},
{"id":"luma/photon-flash-1","catalogId":"luma/photon-flash-1","description":"The image generation request object"},
{"id":"luma/ray-2","catalogId":"luma/ray-2","description":"The generation request object"},
{"id":"luma/ray-flash-2","catalogId":"luma/ray-flash-2","description":"The generation request object"},
{"id":"luma_2/uni-1","catalogId":"luma_2/uni-1","description":"The Luma Agents generation request object"},
{"id":"luma_2/uni-1-max","catalogId":"luma_2/uni-1-max","description":"The Luma Agents generation request object"},
{"id":"meshy/animations","catalogId":"meshy/animations","description":"Request body for Meshy's animation operation (docs.meshy.ai/en/api/animation): apply an animation action to the character produced by a completed rigging task."},
{"id":"meshy/meshy-5","catalogId":"meshy/meshy-5","description":"The Comfy Router input schema for the `meshy/*` text-to-3d models: either a PREVIEW body (`mode: preview`, which generates the untextured mesh) or a REFINE body (`mode: refine`, which textures a preview task's output).\nIt composes the same two branches the v1 `POST /proxy/meshy/openapi/v2/text-to-3d` body does, differing only in `ai_model`: on this route the model comes from the path (`/v2/models/meshy/{model}`), so `ai_model` may be omitted or sent as null and Comfy Router writes the path's model into it before the call is forwarded."},
{"id":"meshy/meshy-6","catalogId":"meshy/meshy-6","description":"The Comfy Router input schema for the `meshy/*` text-to-3d models: either a PREVIEW body (`mode: preview`, which generates the untextured mesh) or a REFINE body (`mode: refine`, which textures a preview task's output).\nIt composes the same two branches the v1 `POST /proxy/meshy/openapi/v2/text-to-3d` body does, differing only in `ai_model`: on this route the model comes from the path (`/v2/models/meshy/{model}`), so `ai_model` may be omitted or sent as null and Comfy Router writes the path's model into it before the call is forwarded."},
{"id":"meshy/meshy-7","catalogId":"meshy/meshy-7","description":"The Comfy Router input schema for the `meshy/*` text-to-3d models: either a PREVIEW body (`mode: preview`, which generates the untextured mesh) or a REFINE body (`mode: refine`, which textures a preview task's output).\nIt composes the same two branches the v1 `POST /proxy/meshy/openapi/v2/text-to-3d` body does, differing only in `ai_model`: on this route the model comes from the path (`/v2/models/meshy/{model}`), so `ai_model` may be omitted or sent as null and Comfy Router writes the path's model into it before the call is forwarded."},
{"id":"meshy/remesh","catalogId":"meshy/remesh","description":"Request body for Meshy's remesh operation (docs.meshy.ai/en/api/remesh): re-topologise or re-format an existing mesh. Supply exactly one of input_task_id or model_url; Meshy requires no single field, so the only structural narrowing is that an empty body is refused."},
{"id":"meshy/rigging","catalogId":"meshy/rigging","description":"Request body for Meshy's rigging operation (docs.meshy.ai/en/api/rigging): fit a skeleton to an existing humanoid mesh. Supply exactly one of input_task_id or model_url; Meshy requires no single field, so the only structural narrowing is that an empty body is refused."},
{"id":"minimax/minimax-h3","catalogId":"minimax/minimax-h3","description":"Comfy Router output schema for the MiniMax v2 (Hailuo 03) models: the terminal `MinimaxV2TaskResultResponse` poll document. The operation is submit-and-poll and Router polls on the caller's behalf, so the body a caller receives is the finished task (`task.status: succeeded`) rather than the `{task_id}` handle the underlying submit returns.\nThe generated video is at `task.content.url`, and `task.content` is only present on a succeeded task. Every other field is MiniMax's own, forwarded unchanged, but that URL is not: the proxy re-hosts the MP4 and answers a Comfy-signed URL valid for 12 hours (falling back to MiniMax's own, shorter-lived link if the re-host fails). Either way it expires, and Router publishes no task-query route to refresh it with, so download promptly rather than storing the link. A failed task carries `task.error` (code and message) instead of `task.content`, and `task.status` also takes the terminal values `failed`, `cancelled` and `expired`.","incompleteReason":"missing-input-schema"},
{"id":"moonvalley/image-to-video","catalogId":"moonvalley/image-to-video","description":"Request body for Moonvalley's image-to-video prompt route: the text-to-video body plus optional per-timestamp keyframe images."},
{"id":"moonvalley/text-to-image","catalogId":"moonvalley/text-to-image","description":"Request body for Moonvalley's text-to-image prompt route. Moonvalley declares no single required field here, so the only structural narrowing is that an empty body is refused."},
{"id":"moonvalley/text-to-video","catalogId":"moonvalley/text-to-video","description":"Request body for Moonvalley's text-to-video prompt route. Moonvalley declares no single required field here, so the only structural narrowing is that an empty body is refused."},
{"id":"moonvalley/video-to-video","catalogId":"moonvalley/video-to-video","description":"Request body for Moonvalley's video-to-video prompt route. A control video and a control type are what distinguish this route from text-to-video, and Moonvalley requires both alongside the prompt."},
{"id":"moonvalley/video-to-video-resize","catalogId":"moonvalley/video-to-video-resize","description":"Request body for Moonvalley's video-to-video resize route: the video-to-video body plus optional frame position, frame resolution and scale 2-tuples."},
{"id":"openai/gpt-4.1","catalogId":"openai/gpt-4.1","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-4.1-mini","catalogId":"openai/gpt-4.1-mini","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-4.1-nano","catalogId":"openai/gpt-4.1-nano","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-4o","catalogId":"openai/gpt-4o","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5","catalogId":"openai/gpt-5","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5-mini","catalogId":"openai/gpt-5-mini","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5-nano","catalogId":"openai/gpt-5-nano","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5.5","catalogId":"openai/gpt-5.5","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5.5-pro","catalogId":"openai/gpt-5.5-pro","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5.6-luna","catalogId":"openai/gpt-5.6-luna","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5.6-sol","catalogId":"openai/gpt-5.6-sol","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-5.6-terra","catalogId":"openai/gpt-5.6-terra","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-6-astra","catalogId":"openai/gpt-6-astra","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/gpt-image-1","catalogId":"openai/gpt-image-1"},
{"id":"openai/gpt-image-1.5","catalogId":"openai/gpt-image-1.5"},
{"id":"openai/gpt-image-2","catalogId":"openai/gpt-image-2"},
{"id":"openai/gpt-image-2.5-flare","catalogId":"openai/gpt-image-2.5-flare"},
{"id":"openai/gpt-image-2.5-sunburst","catalogId":"openai/gpt-image-2.5-sunburst"},
{"id":"openai/o1","catalogId":"openai/o1","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/o1-pro","catalogId":"openai/o1-pro","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/o3","catalogId":"openai/o3","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"openai/o4-mini","catalogId":"openai/o4-mini","description":"The request body Comfy Router accepts for the seventeen OpenAI Responses chat and reasoning models, and the schema `routervalidate.Guard` validates a `POST /v2/models/openai/{model}` call against before it reaches OpenAI.\nIt describes the SHAPE of a Responses request -- which fields exist and what JSON type each carries -- and deliberately does not restate OpenAI's own sub-vocabularies for `input` items, `tools` or `include`. Router forwards the body unchanged, OpenAI extends those vocabularies continuously, and a schema that enumerated them would refuse calls that run today. OpenAI's Responses API reference is authoritative for the contents of those fields; this schema is authoritative for what Router itself will accept.\nFields not listed here are forwarded to OpenAI untouched. Two of them are SETTLED to `false` on the Router surface when a caller names them -- `stream` and `background` -- because Router captures the provider response rather than streaming it, and a streamed or queued document is one this route can neither return nor meter (routerSettledBoolFields, server/middleware/router_model_catalog.go)."},
{"id":"qwen/qwen-image-3.0","catalogId":"qwen/qwen-image-3.0","description":"Request body for a Qwen multimodal image generation or edit. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/qwen/api/v1/services/aigc/multimodal-generation/generation must add `model` to it. One example cannot serve both surfaces - this component is the Router input schema for BOTH qwen spellings, so an example naming either one is refused by the other model's route for naming a different model, while the omitting form is valid on both."},
{"id":"qwen/qwen-image-3.0-pro","catalogId":"qwen/qwen-image-3.0-pro","description":"Request body for a Qwen multimodal image generation or edit. The `example` below is the COMFY ROUTER form and deliberately omits `model`, which Router fills from the `{model}` path segment; a direct v1 call to POST /proxy/qwen/api/v1/services/aigc/multimodal-generation/generation must add `model` to it. One example cannot serve both surfaces - this component is the Router input schema for BOTH qwen spellings, so an example naming either one is refused by the other model's route for naming a different model, while the omitting form is valid on both."},
{"id":"recraft/recraftv2","catalogId":"recraft/recraftv2","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv3","catalogId":"recraft/recraftv3","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4","catalogId":"recraft/recraftv4","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1","catalogId":"recraft/recraftv4_1","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1_pro","catalogId":"recraft/recraftv4_1_pro","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1_pro_vector","catalogId":"recraft/recraftv4_1_pro_vector","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1_utility","catalogId":"recraft/recraftv4_1_utility","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1_utility_pro","catalogId":"recraft/recraftv4_1_utility_pro","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1_utility_pro_vector","catalogId":"recraft/recraftv4_1_utility_pro_vector","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1_utility_vector","catalogId":"recraft/recraftv4_1_utility_vector","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_1_vector","catalogId":"recraft/recraftv4_1_vector","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_pro","catalogId":"recraft/recraftv4_pro","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_styles","catalogId":"recraft/recraftv4_styles","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_styles_pro","catalogId":"recraft/recraftv4_styles_pro","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_styles_pro_vector","catalogId":"recraft/recraftv4_styles_pro_vector","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"recraft/recraftv4_styles_vector","catalogId":"recraft/recraftv4_styles_vector","description":"The Recraft image-generation fields, shared by the v1 `POST /proxy/recraft/image_generation` request body and the Comfy Router input schema for the `recraft/*` models. See the note above this component for why the two surfaces share properties but not `required`.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `RecraftImageGenerationRequest` re-adds it."},
{"id":"runway/aleph2","catalogId":"runway/aleph2","description":"Request to edit an input video into a new video using Runway's API."},
{"id":"runway/gen4_image","catalogId":"runway/gen4_image"},
{"id":"runway/gen4_turbo","catalogId":"runway/gen4_turbo"},
{"id":"tencent/hunyuan-3d-part","catalogId":"tencent/hunyuan-3d-part","description":"Request body for Tencent Hunyuan 3D component generation (part splitting)."},
{"id":"tencent/hunyuan-3d-smart-topology","catalogId":"tencent/hunyuan-3d-smart-topology","description":"Request body for Tencent Hunyuan 3D Smart Topology (retopology/polygon reduction)"},
{"id":"tencent/hunyuan-3d-texture-edit","catalogId":"tencent/hunyuan-3d-texture-edit","description":"Request body for Tencent Hunyuan 3D texture edit"},
{"id":"tencent/hunyuan-3d-uv","catalogId":"tencent/hunyuan-3d-uv","description":"Request body for Tencent Hunyuan 3D UV unfolding."},
{"id":"veo/veo-2.0-generate-001","catalogId":"veo/veo-2.0-generate-001","description":"Request body for the deprecated `POST /proxy/veo/generate` operation, which runs veo-2.0-generate-001. The Veo 2 tier is 720p only, duration 5-8 whole seconds, and silent: it supports neither audio generation nor lastFrame nor referenceImages."},
{"id":"veo/veo-3.0-fast-generate-001","catalogId":"veo/veo-3.0-fast-generate-001"},
{"id":"veo/veo-3.0-generate-001","catalogId":"veo/veo-3.0-generate-001"},
{"id":"veo/veo-3.1-fast-generate-001","catalogId":"veo/veo-3.1-fast-generate-001"},
{"id":"veo/veo-3.1-generate-001","catalogId":"veo/veo-3.1-generate-001"},
{"id":"veo/veo-3.1-lite-generate-001","catalogId":"veo/veo-3.1-lite-generate-001"},
{"id":"vertexai/gemini-2.5-flash","catalogId":"vertexai/gemini-2.5-flash"},
{"id":"vertexai/gemini-2.5-flash-image","catalogId":"vertexai/gemini-2.5-flash-image"},
{"id":"vertexai/gemini-2.5-pro","catalogId":"vertexai/gemini-2.5-pro"},
{"id":"vertexai/gemini-3-pro-image","catalogId":"vertexai/gemini-3-pro-image"},
{"id":"vertexai/gemini-3.1-flash-image","catalogId":"vertexai/gemini-3.1-flash-image"},
{"id":"vertexai/gemini-3.1-flash-lite","catalogId":"vertexai/gemini-3.1-flash-lite"},
{"id":"vertexai/gemini-3.1-flash-lite-image","catalogId":"vertexai/gemini-3.1-flash-lite-image"},
{"id":"vertexai/gemini-3.1-pro-preview","catalogId":"vertexai/gemini-3.1-pro-preview"},
{"id":"vertexai/gemini-3.5-flash","catalogId":"vertexai/gemini-3.5-flash"},
{"id":"vertexai/gemini-3.7-flash","catalogId":"vertexai/gemini-3.7-flash"},
{"id":"vertexai/gemini-3.8-flash","catalogId":"vertexai/gemini-3.8-flash"},
{"id":"vertexai/imagen-3.0-fast-generate-001","catalogId":"vertexai/imagen-3.0-fast-generate-001"},
{"id":"vertexai/imagen-3.0-generate-001","catalogId":"vertexai/imagen-3.0-generate-001"},
{"id":"vertexai/imagen-3.0-generate-002","catalogId":"vertexai/imagen-3.0-generate-002"},
{"id":"wan/happyhorse-1.0-i2v","catalogId":"wan/happyhorse-1.0-i2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/happyhorse-1.0-r2v","catalogId":"wan/happyhorse-1.0-r2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/happyhorse-1.0-t2v","catalogId":"wan/happyhorse-1.0-t2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/happyhorse-1.0-video-edit","catalogId":"wan/happyhorse-1.0-video-edit","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/happyhorse-1.1-i2v","catalogId":"wan/happyhorse-1.1-i2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/happyhorse-1.1-r2v","catalogId":"wan/happyhorse-1.1-r2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/happyhorse-1.1-t2v","catalogId":"wan/happyhorse-1.1-t2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.5-i2i-preview","catalogId":"wan/wan2.5-i2i-preview","description":"The Wan image-to-image fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/image2image/image-synthesis` request body and the Comfy Router input schema for `wan/wan2.5-i2i-preview`.\n`required` here is the ROUTER-SAFE floor; the v1 wrapper `WanImage2ImageGenerationRequest` re-adds `model` for direct v1 calls."},
{"id":"wan/wan2.5-i2v-preview","catalogId":"wan/wan2.5-i2v-preview","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.5-t2i-preview","catalogId":"wan/wan2.5-t2i-preview","description":"The Wan text-to-image fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/text2image/image-synthesis` request body and the Comfy Router input schema for `wan/wan2.5-t2i-preview`.\n`required` here is the ROUTER-SAFE floor; the v1 wrapper `WanImageGenerationRequest` re-adds `model` for direct v1 calls."},
{"id":"wan/wan2.5-t2v-preview","catalogId":"wan/wan2.5-t2v-preview","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.6-i2v","catalogId":"wan/wan2.6-i2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.6-r2v","catalogId":"wan/wan2.6-r2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.6-t2v","catalogId":"wan/wan2.6-t2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.7-i2v","catalogId":"wan/wan2.7-i2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.7-r2v","catalogId":"wan/wan2.7-r2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.7-t2v","catalogId":"wan/wan2.7-t2v","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan2.7-videoedit","catalogId":"wan/wan2.7-videoedit","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan3.0-video","catalogId":"wan/wan3.0-video","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wan/wan3.0-video-prime","catalogId":"wan/wan3.0-video-prime","description":"The Wan / HappyHorse video-generation fields, shared by the v1 `POST /proxy/wan/api/v1/services/aigc/video-generation/video-synthesis` request body and the Comfy Router input schema. This is a shared validation floor, not the complete contract of each provider model. Image-to-video, reference-to-video and editing models need model-specific reference inputs; supported resolutions and duration ranges also vary by model. The proxy additionally requires parameters with a recognized size or resolution for billing. Schema acceptance alone does not imply provider acceptance.\n`required` here is the ROUTER-SAFE floor: `model` is absent from it because Router writes the path's model into the body after routervalidate.Guard has already validated the caller's bytes. The v1 wrapper `WanVideoGenerationRequest` re-adds it."},
{"id":"wavespeed/flashvsr","catalogId":"wavespeed/flashvsr","description":"Request body for WavespeedAI FlashVSR video upscaling"},
{"id":"wavespeed/seedvr2","catalogId":"wavespeed/seedvr2","description":"Request body for WavespeedAI image upscaling. One component serves both wavespeed/seedvr2 and wavespeed/ultimate-image-upscaler, which take the same request shape."},
{"id":"wavespeed/ultimate-image-upscaler","catalogId":"wavespeed/ultimate-image-upscaler","description":"Request body for WavespeedAI image upscaling. One component serves both wavespeed/seedvr2 and wavespeed/ultimate-image-upscaler, which take the same request shape."},
{"id":"xai/grok-imagine-image","catalogId":"xai/grok-imagine-image","description":"Request body for xAI Grok Imagine image generation"},
{"id":"xai/grok-imagine-image-2.0","catalogId":"xai/grok-imagine-image-2.0","description":"Request body for xAI Grok Imagine image generation"},
{"id":"xai/grok-imagine-image-pro","catalogId":"xai/grok-imagine-image-pro","description":"Request body for xAI Grok Imagine image generation"},
{"id":"xai/grok-imagine-image-quality","catalogId":"xai/grok-imagine-image-quality","description":"Request body for xAI Grok Imagine image generation"},
{"id":"xai/grok-imagine-video","catalogId":"xai/grok-imagine-video","description":"Request body for xAI Grok Imagine video generation.\nSupports three modes: text-to-video (prompt only), image-to-video (prompt + image),\nand reference-to-video (prompt + reference_images).\nThe fields image, reference_images, and video are mutually exclusive.\n"},
{"id":"xai/grok-imagine-video-1.5","catalogId":"xai/grok-imagine-video-1.5","description":"Request body for xAI Grok Imagine video generation.\nSupports three modes: text-to-video (prompt only), image-to-video (prompt + image),\nand reference-to-video (prompt + reference_images).\nThe fields image, reference_images, and video are mutually exclusive.\n"},
{"id":"xai/grok-imagine-video-1.5-preview","catalogId":"xai/grok-imagine-video-1.5-preview","description":"Request body for xAI Grok Imagine video generation.\nSupports three modes: text-to-video (prompt only), image-to-video (prompt + image),\nand reference-to-video (prompt + reference_images).\nThe fields image, reference_images, and video are mutually exclusive.\n"}
]
