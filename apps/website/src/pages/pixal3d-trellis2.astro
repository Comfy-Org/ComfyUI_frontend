---
import { cn } from '@comfyorg/tailwind-utils'
import { Copy as CopyIcon, KeyRound } from '@lucide/vue'

import FAQSplit01 from '../components/blocks/FAQSplit01.vue'
import BrandButton from '../components/common/BrandButton.vue'
import GlassCard from '../components/common/GlassCard.vue'
import Badge from '../components/ui/badge/Badge.vue'
import { toggleVariants } from '../components/ui/toggle'
import BaseLayout from '../layouts/BaseLayout.astro'
import { highlightInline } from '../lib/highlight'

const name = 'Image to 3D Model'
const slugId = 'pixal3d-trellis2'
const blurb =
  'Turn a single image into a textured 3D model you can inspect, download, and continue refining on the graph.'
const meta = { label: '3D' }
const dayZero = false
const runs = 'Image to textured mesh'
const price = 'credit estimate before run'
const imageTo3dExamples = [
  {
    label: 'Viking axe',
    name: 'viking_wolf_rune_axe.png',
    imageUrl:
      'https://raw.githubusercontent.com/Comfy-Org/workflow_templates/refs/heads/main/input/viking_wolf_rune_axe.png',
    alt: 'Viking wolf rune axe example input',
    modelUrl:
      'https://media.comfy.org/website/pixal3d-trellis2/viking-wolf-rune-axe.glb'
  },
  {
    label: 'Viking',
    name: 'viking-fisher-input.png',
    imageUrl:
      'https://media.comfy.org/website/pixal3d-trellis2/viking-fisher-input.png',
    alt: 'Viking holding a fish and an axe example input',
    modelUrl:
      'https://media.comfy.org/website/pixal3d-trellis2/viking-fisher-result.glb'
  },
  {
    label: 'Dragon',
    name: 'dragon-hatchling-input.png',
    imageUrl:
      'https://media.comfy.org/website/pixal3d-trellis2/dragon-hatchling-input.png',
    alt: 'Crystal-backed dragon emerging from an egg example input',
    modelUrl:
      'https://media.comfy.org/website/pixal3d-trellis2/dragon-hatchling-result.glb'
  }
]
const defaultImageTo3dExample = imageTo3dExamples[0]
const imageTo3dModelUrl = defaultImageTo3dExample?.modelUrl ?? null
const imageTo3dWorkflowUrl = 'https://cloud.comfy.org/?share=920c795f693a'
const baseCredits = 6
const kindLabel = 'Open weights'
const sentence = (s: string) => s.charAt(0).toUpperCase() + s.slice(1)
const pill =
  'inline-flex h-6 shrink-0 items-center rounded-full px-4 text-xs font-normal transition-colors'
const pillMuted = 'bg-transparency-white-t4 text-primary-comfy-canvas'
const pillActive = 'bg-transparency-white-t8 text-primary-comfy-canvas'
const pillIdle =
  'text-primary-warm-gray hover:bg-transparency-white-t4 hover:text-primary-comfy-canvas'
const sectionTitle =
  'text-xs font-bold uppercase tracking-widest text-primary-comfy-yellow'
const cardTitle =
  'text-sm font-bold uppercase tracking-widest text-primary-comfy-yellow'
const bodyCopy = 'text-sm font-light text-primary-warm-gray'
const helperCopy = 'text-2xs font-light text-primary-warm-gray'
const fieldLabel =
  'block text-2xs font-bold uppercase tracking-widest text-primary-warm-gray'
const textAction =
  'text-2xs font-bold uppercase tracking-widest text-primary-warm-gray hover:text-primary-warm-white'
const iconButton =
  'group shrink-0 cursor-pointer rounded-lg border border-transparency-white-t20 bg-transparency-white-t4 p-1.5 text-primary-warm-gray transition-colors hover:bg-transparency-white-t8 hover:text-primary-warm-white'
const tab = cn(
  toggleVariants(),
  'rounded-none px-4 first:rounded-l-xl last:rounded-r-xl'
)

const canonicalURL = new URL(
  '/pixal3d-trellis2/',
  Astro.site ?? 'https://comfy.org'
)
const sdkExamples = [
  {
    id: 'python',
    label: 'Python',
    language: 'python',
    code: `# pip install comfy-sdk
import os
from comfy_sdk import Comfy
os.environ["COMFY_BASE_URL"] = "https://cloud.comfy.org"
client = Comfy(api_key=os.environ["COMFY_API_KEY"])
wf = client.workflows.from_file("workflow_api.json")

# Upload an asset and set it as an input to the workflow
asset = client.assets.from_file("viking_wolf_rune_axe.png")
asset.commit()
wf.set_input("122", "image", asset)

# Submit the workflow and download the output
job = client.run(wf)
out = job.get_outputs("321")[0]
out.to_file("viking_wolf_rune_axe_output.glb")`
  },
  {
    id: 'javascript',
    label: 'JavaScript',
    language: 'javascript',
    code: `// npm install @comfyorg/sdk  (Node 22+)
import { Comfy } from "@comfyorg/sdk";

process.env.COMFY_BASE_URL = "https://cloud.comfy.org";
const client = new Comfy({ apiKey: process.env.COMFY_API_KEY });
const wf = await client.workflows.fromFile("workflow_api.json");

// Upload an asset and set it as an input to the workflow
const asset = await client.assets.fromFile("viking_wolf_rune_axe.png");
await asset.commit();
wf.setInput("122", "image", asset);

// Submit the workflow and download the output
const job = await client.run(wf);
const out = job.getOutputs("321")[0];
await out.toFile("viking_wolf_rune_axe_output.glb");`
  }
] as const
const highlightedSdkExamples = sdkExamples.map((example) => ({
  ...example,
  html: highlightInline(example.code, example.language)
}))
const softwareAppJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name,
  applicationCategory: 'MultimediaApplication',
  operatingSystem: 'Any',
  url: canonicalURL.href,
  author: {
    '@type': 'Organization',
    name: 'Comfy Org',
    url: 'https://comfy.org'
  }
}
const breadcrumbJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    {
      '@type': 'ListItem',
      position: 1,
      name: 'Home',
      item: 'https://comfy.org'
    },
    { '@type': 'ListItem', position: 2, name, item: canonicalURL.href }
  ]
}
const faqs = [
  {
    id: 'what-is-trellis-2',
    question: 'What is Trellis.2?',
    answer:
      "Trellis.2 is Microsoft's open-source image-to-3D model, released in December 2025. It is a 4-billion-parameter model built on a compact O-Voxel structured latent representation that generates geometry and PBR materials from a single image at effective resolutions up to 1536³. It now runs natively in ComfyUI core."
  },
  {
    id: 'what-is-pixal3d',
    question: 'What is Pixal3D?',
    answer:
      'Pixal3D is an image-to-3D model from Tsinghua University and Tencent ARC Lab. It uses pixel-aligned generation to keep the 3D output in direct correspondence with the input view, producing near-reconstruction-level fidelity. It is built on the Trellis.2 backbone and runs natively in ComfyUI alongside it.'
  },
  {
    id: 'commercial-use',
    question: 'Can I use Trellis.2 and Pixal3D output commercially?',
    answer:
      "Yes. Trellis.2's code and weights are MIT-licensed, and ComfyUI's native implementation replaces the non-commercial nvdiffrast and nvdiffrec dependencies from the original pipeline with a from-scratch PyTorch and SciPy reimplementation. There are no non-commercial dependencies in the ComfyUI pipeline, so generated assets can be used in commercial work."
  },
  {
    id: 'pixal3d-vs-trellis-2',
    question: 'How does Pixal3D compare to Trellis.2?',
    answer:
      'Pixal3D shares the Trellis.2 backbone, VAEs, and DINOv3 image conditioning, and adds pixel-aligned generation, explicit image back-projection into a 3D feature volume, and cascaded refinement of structure, shape, and texture. It generally produces better results than Trellis.2, with tighter fidelity to the input image. Both output the same PBR material set.'
  },
  {
    id: 'cost',
    question: 'How much does it cost to use Trellis.2 or Pixal3D in ComfyUI?',
    answer:
      'Nothing per asset. Both models are open weights and can run locally on consumer hardware as part of ComfyUI core, so there is no per-generation charge. They are also available on Comfy Cloud, where standard Comfy Cloud credit usage applies.'
  },
  {
    id: 'pytorch-cuda-version',
    question: 'Do I need a specific PyTorch or CUDA version?',
    answer:
      'No. The original Trellis.2 pipeline required PyTorch 2.6.0 with CUDA 12.4 and a stack of compiled extensions. The native ComfyUI implementation drops all of them. If your ComfyUI runs, these models run on your current PyTorch.'
  },
  {
    id: 'inputs',
    question: 'What inputs do Trellis.2 and Pixal3D take?',
    answer:
      'A single image. Both models generate geometry and materials from one input view. The workflows also include BiRefNet for background removal and MoGe for camera field-of-view estimation, both available on the Comfy-Org Hugging Face page.'
  },
  {
    id: 'model-weights',
    question: 'Which model weights do I need to download?',
    answer:
      'Comfy-Org/TRELLIS.2 and Comfy-Org/Pixal3D from Hugging Face, plus Comfy-Org/BiRefNet for background removal and Comfy-Org/MoGe for camera FOV estimation. The workflow note lists the target directory for each.'
  },
  {
    id: 'texture-maps',
    question: 'What texture maps does the ComfyUI pipeline generate?',
    answer:
      "Trellis.2's texturing model produces base color, roughness, and metallic maps. The ComfyUI implementation adds a UV unwrapping node and bakes two more maps from the high-poly mesh: a normal map and an ambient occlusion map, giving a complete PBR material set."
  },
  {
    id: 'mesh-post-processing',
    question: 'What mesh post-processing nodes are included?',
    answer:
      'Four new nodes ship with this release. Remesh Mesh fixes holes and mesh imperfections, Decimate Mesh reduces face and vertex count to a target budget, Smooth Mesh Normals smooths the mesh volume, and Render Mesh produces a quick image render. The Load 3D, Preview 3D, and Save 3D nodes were also rebuilt for these workflows.'
  }
]
const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map(({ question, answer }) => ({
    '@type': 'Question',
    name: question,
    acceptedAnswer: { '@type': 'Answer', text: answer }
  }))
}
---

<BaseLayout
  title="Pixal3D + TRELLIS.2 in ComfyUI"
  description="Turn one image into a textured 3D model with Pixal3D and TRELLIS.2 in ComfyUI. Explore examples, download GLBs, and open the workflow."
>
  <Fragment slot="head">
    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify(softwareAppJsonLd)}
    ></script>
    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify(breadcrumbJsonLd)}
    ></script>
    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify(faqJsonLd)}
    ></script></Fragment>
  <!-- COMPACT HEADER -->
  <section class="mx-auto max-w-9xl px-5 pt-8 sm:px-6 lg:px-20 lg:pt-12">
    <div class="flex flex-wrap items-center justify-between gap-3">
      <div class="flex min-w-0 flex-wrap items-center gap-3">
        <h1
          class="max-w-full truncate text-2xl font-semibold text-primary-warm-white sm:text-3xl"
        >
          {name}
        </h1>
        <Badge variant="accent">{meta.label}</Badge>
      </div>
      <div class="hidden gap-2 lg:flex"></div>
    </div>
    <p class="mt-2 max-w-3xl text-sm text-primary-comfy-canvas lg:text-base">
      {blurb}
    </p>
    <div class="mt-4 flex flex-wrap gap-1.5">
      {dayZero && <span class:list={[pill, pillMuted]}>Day zero</span>}
      <span class:list={[pill, pillMuted]}>{kindLabel}</span>
      <span class:list={[pill, pillMuted]}>Commercial use</span>
      <span class:list={[pill, pillMuted]}>{sentence(price)}</span>
      <span class:list={[pill, pillMuted]}>{sentence(runs)}</span>
    </div>
  </section>

  <!-- TABS + TRIO -->
  <section class="mx-auto max-w-9xl px-5 pt-16 sm:px-6 lg:px-20">
    <div class="flex flex-wrap items-center justify-between gap-4">
      <nav
        class="flex w-fit max-w-full items-center gap-0.5 overflow-x-auto rounded-2xl p-1.5 ring-2 ring-primary-warm-white/20"
        role="tablist"
        aria-label="Model page sections"
      >
        <button
          type="button"
          role="tab"
          data-tab="playground"
          data-state="on"
          aria-controls="panel-playground"
          aria-selected="true"
          class={tab}
        >
          <span class="ppformula-text-center">Examples</span>
        </button>
        <button
          type="button"
          role="tab"
          data-tab="api"
          data-state="off"
          aria-controls="panel-api"
          aria-selected="false"
          class={tab}
        >
          <span class="ppformula-text-center">API</span>
        </button>
      </nav>
    </div>
  </section>

  <!-- PLAYGROUND -->
  <section
    id="panel-playground"
    data-panel="playground"
    role="tabpanel"
    class="mx-auto max-w-9xl px-5 pt-6 sm:px-6 lg:px-20"
  >
    <div
      id="signed-in-strip"
      class="mb-4 hidden items-center justify-between gap-3 rounded-xl border border-primary-comfy-yellow/40 bg-primary-comfy-yellow/10 px-4 py-2.5 sm:flex-nowrap"
    >
      <p
        class="text-2xs font-bold tracking-widest text-primary-comfy-yellow uppercase"
      >
        Signed in · inputs restored — no survey, no plan picker
      </p>
      <p
        id="free-meter"
        class="shrink-0 text-2xs font-bold tracking-widest text-primary-comfy-yellow uppercase"
      >
        Free runs: 5 left
      </p>
    </div>

    <GlassCard class="grid gap-2 lg:grid-cols-2">
      <!-- INPUT CARD -->
      <div class="rounded-4xl bg-primary-comfy-ink p-5 sm:p-6">
        <div class="flex items-center justify-between">
          <h2 class={cardTitle}>Input</h2>
        </div>

        <!-- APP VIEW — the workflow's exposed inputs, rendered as an app -->
        <div id="panel-app">
          <div class="mt-5">
            <span class={fieldLabel}>Choose an example</span>
            <div id="example-images" class="mt-2 grid grid-cols-3 gap-2">
              {imageTo3dExamples.map((example, index) => <button
                type="button"
                data-example-image
                data-example-name={example.name}
                data-example-url={example.imageUrl}
                data-example-alt={example.alt}
                data-example-model-url={example.modelUrl}
                data-selected={index === 0 ? '' : undefined}
                aria-label={`Use ${example.label} example`}
                aria-pressed={index === 0 ? 'true' : 'false'}
                class="group min-w-0 cursor-pointer rounded-xl border border-transparency-white-t20 bg-transparency-white-t4 p-1.5 text-left transition-colors hover:border-primary-comfy-yellow/40 hover:bg-transparency-white-t8 focus:border-primary-comfy-yellow/60 focus:outline-none data-selected:border-primary-comfy-yellow/60 data-selected:bg-transparency-white-t8"
              >
                <span
                  class="block aspect-square overflow-hidden rounded-lg bg-transparency-white-t8"
                >
                  <img
                    src={example.imageUrl}
                    alt=""
                    class="size-full object-cover transition-transform group-hover:scale-[1.02]"
                    loading={index === 0 ? 'eager' : 'lazy'}
                  />
                </span>
                <span
                  class="block truncate px-1 pt-2 pb-0.5 text-2xs font-normal text-primary-comfy-canvas group-data-selected:text-primary-comfy-yellow"
                  >{example.label}</span
                >
              </button>)}
            </div>
          </div>
          <p
            id="image-error"
            class="mt-2 hidden text-xs text-primary-comfy-orange"
            role="alert"
          >
            Add an image before running the workflow.
          </p>
          <p
            id="run-error"
            class="mt-2 hidden text-xs text-primary-comfy-orange"
            role="alert"
          ></p>
        </div>

        <!-- JSON VIEW -->
        <div id="panel-json" class="hidden">
          <div
            class="mt-4 overflow-hidden rounded-2xl border border-transparency-white-t20"
          >
            <div
              class="flex items-center justify-between border-b border-transparency-white-t20 px-4 py-2"
            >
              <div class="flex items-center gap-2 text-primary-warm-gray">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="size-3.5"
                >
                  <path
                    d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"
                  />
                  <path d="M14 2v6h6" />
                  <path d="m10 13-2 2 2 2" />
                  <path d="m14 17 2-2-2-2" />
                </svg>
                <span class="font-mono text-2xs">json</span>
              </div>
              <button
                type="button"
                data-copy="input-json"
                title="Copy"
                aria-label="Copy"
                class={iconButton}
              >
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="size-3.5 group-data-copied:hidden"
                >
                  <rect width="14" height="14" x="8" y="8" rx="2" ry="2" />
                  <path
                    d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"
                  />
                </svg
                ><svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="hidden size-3.5 group-data-copied:block"
                >
                  <path d="M20 6 9 17l-5-5" />
                </svg>
              </button>
            </div>
            <pre
              id="input-json"
              class="max-h-96 scrollbar-code overflow-auto p-4 font-mono text-xs/relaxed wrap-break-word whitespace-pre-wrap text-primary-warm-gray"
            ></pre>
          </div>
          <p class:list={[helperCopy, 'mt-2']}>
            Live from your inputs — the exact payload the API takes.
          </p>
        </div>

        <div
          class="mt-5 flex items-center justify-between gap-3 border-t border-white/10 pt-4"
        >
          <div>
            <p class="text-sm font-bold text-primary-comfy-canvas">
              Continue in Comfy Cloud
            </p>
            <p class="mt-1 text-xs text-primary-comfy-canvas">
              Open the shared workflow to generate your own 3D model.
            </p>
          </div>
          <div class="ml-auto flex items-center gap-3">
            <BrandButton
              id="pg-run-desktop"
              variant="solid"
              size="xs"
              class="tracking-widest uppercase"
              >TRY WORKFLOW</BrandButton
            >
          </div>
        </div>
      </div>

      <!-- RESULT CARD -->
      <div class="flex flex-col rounded-4xl bg-primary-comfy-ink p-5 sm:p-6">
        <div class="flex items-center justify-between">
          <h2 class={cardTitle}>Result</h2>
          <div id="rview-toggle" class="flex gap-1.5">
            <button data-rview="preview" class:list={[pill, pillActive]}>
              Preview
            </button>
            <button data-rview="json" class:list={[pill, pillIdle]}>
              Code
            </button>
          </div>
        </div>
        <div
          class="relative mt-5 aspect-video w-full overflow-hidden rounded-2xl"
        >
          <div
            id="result-preview"
            class="relative size-full bg-primary-comfy-ink-light"
          >
            <canvas
              id="result-model-canvas"
              data-model-url={imageTo3dModelUrl}
              aria-label={`${name} interactive 3D result. Drag to orbit and scroll to zoom.`}
              class="size-full cursor-grab touch-none active:cursor-grabbing"
            ></canvas>
            <p
              id="model-viewer-status"
              class="pointer-events-none absolute inset-0 flex items-center justify-center text-xs text-primary-warm-gray"
            >
              Loading 3D preview…
            </p>
            <span
              class="pointer-events-none absolute bottom-3 left-3 rounded-full bg-primary-comfy-ink/80 px-3 py-1.5 text-2xs text-primary-comfy-canvas backdrop-blur-sm"
              >Drag to orbit · scroll to zoom</span
            >
          </div>
          <div
            id="result-json-box"
            class="absolute inset-0 hidden overflow-hidden rounded-2xl border border-transparency-white-t20"
          >
            <div class="flex h-full flex-col">
              <div
                class="flex items-center justify-between border-b border-transparency-white-t20 px-4 py-2"
              >
                <div class="flex items-center gap-2 text-primary-warm-gray">
                  <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    class="size-3.5"
                  >
                    <path
                      d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"
                    />
                    <path d="M14 2v6h6" />
                    <path d="m10 13-2 2 2 2" />
                    <path d="m14 17 2-2-2-2" />
                  </svg>
                  <span class="font-mono text-2xs">json</span>
                </div>
                <div class="flex items-center gap-2">
                  <button
                    type="button"
                    data-copy="result-json"
                    title="Copy"
                    aria-label="Copy"
                    class={iconButton}
                  >
                    <svg
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      class="size-3.5 group-data-copied:hidden"
                    >
                      <rect width="14" height="14" x="8" y="8" rx="2" ry="2" />
                      <path
                        d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"
                      />
                    </svg
                    ><svg
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      class="hidden size-3.5 group-data-copied:block"
                    >
                      <path d="M20 6 9 17l-5-5" />
                    </svg>
                  </button>
                  <button
                    type="button"
                    data-download="result-json"
                    data-filename={`${slugId}-result.json`}
                    title="Download JSON"
                    aria-label="Download JSON"
                    class={iconButton}
                  >
                    <svg
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      class="size-3.5"
                    >
                      <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
                      <path d="m7 10 5 5 5-5" />
                      <path d="M12 15V3" />
                    </svg>
                  </button>
                </div>
              </div>
              <pre
                id="result-json"
                class="min-h-0 scrollbar-code flex-1 overflow-auto p-4 font-mono text-xs/relaxed wrap-break-word whitespace-pre-wrap text-primary-warm-gray"
              ></pre>
            </div>
          </div>
          <div
            id="running-overlay"
            class="absolute inset-0 hidden flex-col items-center justify-center gap-4 bg-primary-comfy-ink/95 px-8 text-center"
          >
            <p class={sectionTitle}>Restored your image ✓</p>
            <p class="text-lg font-semibold text-primary-warm-white">
              Running the graph
            </p>
            <div
              class="h-2 w-56 overflow-hidden rounded-full bg-transparency-white-t20 sm:w-72"
            >
              <div
                id="progress-fill"
                class="h-full w-0 rounded-full bg-primary-comfy-yellow transition-[width] duration-300 ease-out"
              ></div>
            </div>
            <p class="font-mono text-2xs text-primary-warm-gray">
              ksampler · queue position 1
            </p>
          </div>
          <div
            id="upgrade-overlay"
            class="absolute inset-0 hidden flex-col justify-center gap-3 overflow-y-auto bg-primary-comfy-ink/97 p-6 sm:px-10"
          >
            <p class={sectionTitle}>That was your fifth free run</p>
            <p
              class="text-xl font-semibold text-primary-warm-white sm:text-2xl"
            >
              Keep running it.
            </p>
            <div class="divide-y divide-white/10 border-y border-white/10">
              <div class="flex items-center justify-between gap-4 py-2.5">
                <span
                  class="text-xs font-bold tracking-widest text-primary-warm-white uppercase"
                  >Creator — $20/mo · est.</span
                >
                <span class="text-2xs font-light text-primary-comfy-canvas"
                  >Textured GLB output included</span
                >
              </div>
              <div class="flex items-center justify-between gap-4 py-2.5">
                <span
                  class="text-xs font-bold tracking-widest text-primary-warm-white uppercase"
                  >Pro — $60/mo · est.</span
                >
                <span class="text-2xs font-light text-primary-comfy-canvas"
                  >≈ 800 runs · API keys · batch queues</span
                >
              </div>
            </div>
            <p class="text-xs/relaxed font-light text-primary-warm-gray">
              Your graphs, seeds, history, and all five outputs stay yours —
              free, forever. Or run it locally: it’s open source.
            </p>
            <div class="flex flex-wrap items-center gap-4">
              <BrandButton
                variant="solid"
                size="xs"
                class="tracking-widest uppercase"
                >Go Creator</BrandButton
              >
              <span
                class="text-2xs font-bold tracking-widest text-primary-comfy-canvas uppercase"
                >See all plans →</span
              >
              <button id="demo-reset" class={textAction}>Replay demo</button>
            </div>
          </div>
        </div>
        <div
          id="result-meta"
          class="flex max-w-full flex-wrap items-center gap-2 pt-3"
        >
          <><span class:list={[pill, pillMuted]}>Textured GLB</span
            ><span id="result-output-name" class:list={[pill, pillMuted]}
              >Viking axe example output</span
            ></>
        </div>
        <div class="mt-auto flex justify-end pt-3">
          <div id="result-download" class="shrink-0">
            <BrandButton
              id="result-model-download"
              href={imageTo3dModelUrl}
              download={`${slugId}-result.glb`}
              variant="outline"
              size="xs"
              class="tracking-widest uppercase"
              >Download GLB</BrandButton
            >
          </div>
        </div>
      </div>
    </GlassCard>
  </section>

  <!-- API -->
  <section
    id="panel-api"
    data-panel="api"
    role="tabpanel"
    hidden
    class="mx-auto max-w-9xl scroll-mt-24 px-5 py-12 sm:px-6 lg:px-20 lg:pb-24"
  >
    <h2 class={sectionTitle}>Run it your way</h2>
    <p class:list={[bodyCopy, 'mt-2']}>
      Create a key, then make your first call with the SDK.
    </p>
    <div
      id="sdk-examples"
      class="mt-5 overflow-hidden rounded-2xl border border-transparency-white-t20 bg-primary-comfy-ink"
    >
      <div
        class="flex items-center justify-between gap-3 border-b border-transparency-white-t20 p-2"
      >
        <div
          class="flex items-center gap-1"
          role="tablist"
          aria-label="SDK language"
        >
          {highlightedSdkExamples.map((example, index) => <button
            type="button"
            role="tab"
            data-sdk-tab={example.id}
            data-state={index === 0 ? 'on' : 'off'}
            aria-selected={index === 0 ? 'true' : 'false'}
            class:list={[pill, index === 0 ? pillActive : pillIdle]}
          >
            {example.label}
          </button>)}
        </div>
        <button
          id="sdk-copy"
          type="button"
          data-copy="sdk-python"
          class="group flex cursor-pointer items-center gap-2 rounded-lg px-3 py-2 text-xs text-primary-warm-gray transition-colors hover:bg-transparency-white-t4 hover:text-primary-warm-white"
        >
          <CopyIcon
            class="size-4 group-data-copied:hidden"
            aria-hidden="true"
          />
          <span class="ppformula-text-center group-data-copied:hidden"
            >Copy</span
          >
          <span
            class="inline-block text-primary-comfy-yellow group-data-copied:inline"
            >Copied</span
          >
        </button>
      </div>
      {highlightedSdkExamples.map((example, index) => <pre
        id={`sdk-${example.id}`}
        data-sdk-panel={example.id}
        class:list={[
          'min-h-52 scrollbar-code overflow-auto p-5 font-mono text-xs/relaxed wrap-break-word whitespace-pre-wrap text-primary-warm-gray sm:p-6',
          index !== 0 && 'hidden'
        ]}
        set:html={example.html}
      ></pre>)}
    </div>
    <div class="mt-4 flex flex-wrap items-center gap-4">
      <BrandButton
        href="https://platform.comfy.org/profile/api-keys"
        target="_blank"
        variant="solid"
        size="sm"
        class="gap-2 tracking-widest uppercase"
      >
        <span class="inline-flex items-center gap-2">
          Create API key
          <KeyRound class="size-4" aria-hidden="true" />
        </span>
      </BrandButton>
      <a
        href="https://docs.comfy.org/api-reference/v2/overview"
        target="_blank"
        rel="noopener noreferrer"
        class="block py-1.5 text-sm transition-colors hover:text-primary-warm-white"
      >
        API reference
        <img
          src="/icons/arrow-up-right.svg"
          alt=""
          class="inline-block size-3"
          aria-hidden="true"
        />
      </a>
    </div>
  </section>

  <FAQSplit01
    id="model-faq"
    heading="Frequently asked questions"
    faqs={faqs}
    client:visible
  />

  <!-- MOBILE STICKY RUN BAR -->
  <div
    class="fixed inset-x-0 bottom-0 z-40 border-t border-white/10 bg-primary-comfy-ink/95 px-4 pt-3 pb-[calc(env(safe-area-inset-bottom)+12px)] backdrop-blur-sm lg:hidden"
  >
    <BrandButton
      id="pg-run-mobile"
      data-base-credits={baseCredits}
      data-slug={slugId}
      data-workflow-kind="image-to-3d"
      data-default-image-name={defaultImageTo3dExample?.name}
      data-default-image-url={defaultImageTo3dExample?.imageUrl}
      data-default-image-alt={defaultImageTo3dExample?.alt}
      data-default-model-url={defaultImageTo3dExample?.modelUrl}
      data-workflow-url={imageTo3dWorkflowUrl}
      variant="solid"
      size="xs"
      class="w-full py-3.5 tracking-widest uppercase"
      >TRY WORKFLOW</BrandButton
    >
    <p class:list={[helperCopy, 'mt-1.5 text-center']}>
      Open this workflow in Comfy Cloud
    </p>
  </div>
  <div class="h-24 lg:hidden"></div>

  <!-- AUTH WALL MODAL -->
  <div
    id="auth-modal"
    class="fixed inset-0 z-60 hidden items-end justify-center bg-black/70 p-0 backdrop-blur-sm sm:items-center sm:p-6"
  >
    <div
      class="w-full max-w-md rounded-t-3xl border border-transparency-white-t20 bg-site-dropdown p-6 shadow-2xl sm:rounded-3xl sm:p-8"
    >
      <span
        class:list={[
          pill,
          'bg-primary-comfy-yellow/15 text-primary-comfy-yellow'
        ]}
        >This run: <span id="modal-credits">{baseCredits}</span> credits — free
        for you</span
      >
      <h3 class="mt-4 text-2xl font-semibold text-primary-warm-white">
        5 free runs. No card.
      </h3>
      <p class="mt-2 text-sm/relaxed font-light text-primary-comfy-canvas">
        Your image is saved. The run starts the moment you’re in.
      </p>
      <div class="mt-5 flex flex-col gap-2.5">
        <BrandButton data-auth="google" variant="solid" size="xs" class="w-full"
          >Continue with Google</BrandButton
        >
        <BrandButton
          data-auth="github"
          variant="outline-light"
          size="sm"
          class="w-full"
          >Continue with GitHub</BrandButton
        >
        <div class="grid gap-2 sm:grid-cols-2">
          <input
            id="auth-email"
            type="email"
            autocomplete="email"
            placeholder="Email"
            aria-label="Email"
            class="min-w-0 rounded-xl border border-transparency-white-t20 bg-transparency-white-t4 px-3 py-2.5 text-sm text-primary-warm-white"
          />
          <input
            id="auth-password"
            type="password"
            autocomplete="current-password"
            placeholder="Password"
            aria-label="Password"
            class="min-w-0 rounded-xl border border-transparency-white-t20 bg-transparency-white-t4 px-3 py-2.5 text-sm text-primary-warm-white"
          />
        </div>
        <BrandButton
          data-auth="email"
          variant="outline-light"
          size="sm"
          class="w-full"
          >Continue with email</BrandButton
        >
      </div>
      <p id="auth-error" class="text-danger mt-3 hidden text-sm"></p>
      <p class:list={[helperCopy, 'mt-4 leading-relaxed']}>
        By continuing you agree to the Terms. Open source stays open — your
        graphs and outputs are yours.
      </p>
      <button id="auth-close" class:list={[textAction, 'mt-3']}>Not now</button>
    </div>
  </div>

  <script>
    import type { Object3D } from 'three'
    type Pixal3dImage = {
      name: string
      source: string
    }

    function initModelTabs() {
      const tabs = Array.from(
        document.querySelectorAll<HTMLElement>('[data-tab]')
      )
      const panels = Array.from(
        document.querySelectorAll<HTMLElement>('[data-panel]')
      )
      if (!tabs.length || tabs[0].dataset.bound === '1') return
      tabs.forEach((t) => (t.dataset.bound = '1'))

      const names = tabs.map((t) => t.dataset.tab)
      const show = (name: string | undefined) => {
        const active = name && names.includes(name) ? name : 'playground'
        tabs.forEach((t) => {
          const selected = t.dataset.tab === active
          t.setAttribute('aria-selected', String(selected))
          t.dataset.state = selected ? 'on' : 'off'
        })
        panels.forEach((p) => (p.hidden = p.dataset.panel !== active))
      }

      tabs.forEach((t) =>
        t.addEventListener('click', () => {
          show(t.dataset.tab)
          history.replaceState(
            null,
            '',
            t.dataset.tab === 'playground'
              ? location.pathname
              : `#${t.dataset.tab}`
          )
        })
      )

      const toTop = () => window.scrollTo(0, 0)

      const showFromHash = () => {
        show(location.hash.slice(1))
        if (!location.hash) return
        toTop()
        requestAnimationFrame(toTop)
        window.addEventListener('load', toTop, { once: true })
      }

      showFromHash()
      window.addEventListener('hashchange', showFromHash)
    }

    function initCodeCopy() {
      const buttons = Array.from(
        document.querySelectorAll<HTMLElement>('[data-copy]')
      )
      if (!buttons.length || buttons[0].dataset.bound === '1') return
      buttons.forEach((b) => (b.dataset.bound = '1'))

      buttons.forEach((b) =>
        b.addEventListener('click', async () => {
          const text = document.getElementById(b.dataset.copy!)?.textContent
          if (!text) return
          try {
            await navigator.clipboard.writeText(text)
            b.dataset.copied = ''
            window.setTimeout(() => delete b.dataset.copied, 1200)
          } catch {
            /* clipboard unavailable */
          }
        })
      )

      document
        .querySelectorAll<HTMLElement>('[data-download]')
        .forEach((button) =>
          button.addEventListener('click', () => {
            const text = document.getElementById(
              button.dataset.download!
            )?.textContent
            if (!text) return
            const url = URL.createObjectURL(
              new Blob([text], { type: 'application/json' })
            )
            const link = document.createElement('a')
            link.href = url
            link.download = button.dataset.filename ?? 'result.json'
            link.click()
            URL.revokeObjectURL(url)
          })
        )
    }

    function initSdkTabs() {
      const root = document.getElementById('sdk-examples')
      if (!root || root.dataset.bound === '1') return
      root.dataset.bound = '1'

      const tabs = root.querySelectorAll<HTMLElement>('[data-sdk-tab]')
      const panels = root.querySelectorAll<HTMLElement>('[data-sdk-panel]')
      const copy = document.getElementById('sdk-copy')
      for (const tab of tabs) {
        tab.addEventListener('click', () => {
          const selected = tab.dataset.sdkTab
          if (!selected) return
          for (const candidate of tabs) {
            const active = candidate === tab
            candidate.dataset.state = active ? 'on' : 'off'
            candidate.setAttribute('aria-selected', String(active))
            candidate.classList.toggle('bg-transparency-white-t8', active)
            candidate.classList.toggle('text-primary-comfy-canvas', active)
            candidate.classList.toggle('text-primary-warm-gray', !active)
          }
          for (const panel of panels) {
            panel.classList.toggle(
              'hidden',
              panel.dataset.sdkPanel !== selected
            )
          }
          if (copy) copy.dataset.copy = `sdk-${selected}`
        })
      }
    }

    async function initModelViewer() {
      const canvas = document.getElementById(
        'result-model-canvas'
      ) as HTMLCanvasElement | null
      if (!canvas || canvas.dataset.bound === '1') return
      canvas.dataset.bound = '1'

      const status = document.getElementById('model-viewer-status')
      const modelUrl = canvas.dataset.modelUrl
      if (!modelUrl) return

      try {
        const [THREE, { OrbitControls }, { GLTFLoader }] = await Promise.all([
          import('three'),
          import('three/examples/jsm/controls/OrbitControls.js'),
          import('three/examples/jsm/loaders/GLTFLoader.js')
        ])
        if (!canvas.isConnected) return

        const renderer = new THREE.WebGLRenderer({
          alpha: true,
          antialias: true,
          canvas
        })
        renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2))
        renderer.outputColorSpace = THREE.SRGBColorSpace
        renderer.toneMapping = THREE.ACESFilmicToneMapping
        renderer.toneMappingExposure = 1.35

        const scene = new THREE.Scene()
        const camera = new THREE.PerspectiveCamera(35, 1, 0.01, 1000)
        const controls = new OrbitControls(camera, canvas)
        controls.enableDamping = true
        controls.autoRotate = true
        controls.autoRotateSpeed = 1

        scene.add(new THREE.HemisphereLight(0xffffff, 0x555555, 3.5))
        const keyLight = new THREE.DirectionalLight(0xffffff, 4.5)
        keyLight.position.set(4, 6, 5)
        scene.add(keyLight)
        const fillLight = new THREE.DirectionalLight(0xffffff, 2)
        fillLight.position.set(-4, 2, -3)
        scene.add(fillLight)

        let model: Object3D | null = null
        let loadVersion = 0

        const disposeModel = () => {
          if (!model) return
          scene.remove(model)
          model.traverse((object) => {
            if (!(object instanceof THREE.Mesh)) return
            object.geometry.dispose()
            const materials = Array.isArray(object.material)
              ? object.material
              : [object.material]
            materials.forEach((material) => material.dispose())
          })
          model = null
        }

        const loadModel = async (url: string) => {
          const version = ++loadVersion
          status?.classList.remove('hidden')
          if (status) status.textContent = 'Loading 3D preview…'
          const { scene: nextModel } = await new GLTFLoader().loadAsync(url)
          if (version !== loadVersion || !canvas.isConnected) return

          disposeModel()
          model = nextModel
          const bounds = new THREE.Box3().setFromObject(model)
          const size = bounds.getSize(new THREE.Vector3())
          const center = bounds.getCenter(new THREE.Vector3())
          const diameter = Math.max(size.x, size.y, size.z)
          model.position.sub(center)
          scene.add(model)

          camera.near = diameter / 100
          camera.far = diameter * 100
          camera.position.set(diameter * 1.15, diameter * 0.75, diameter * 1.35)
          camera.updateProjectionMatrix()
          controls.target.set(0, 0, 0)
          controls.update()
          status?.classList.add('hidden')
        }

        const onModelUrlChange = (event: Event) => {
          const { url } = (event as CustomEvent<{ url: string }>).detail
          void loadModel(url)
        }
        canvas.addEventListener('model-url-change', onModelUrlChange)
        await loadModel(modelUrl)

        const resize = () => {
          const width = canvas.clientWidth
          const height = canvas.clientHeight
          if (!width || !height) return
          renderer.setSize(width, height, false)
          camera.aspect = width / height
          camera.updateProjectionMatrix()
        }
        const resizeObserver = new ResizeObserver(resize)
        resizeObserver.observe(canvas)
        resize()

        let frame = 0
        const render = () => {
          controls.update()
          renderer.render(scene, camera)
          frame = requestAnimationFrame(render)
        }
        render()

        document.addEventListener(
          'astro:before-swap',
          () => {
            cancelAnimationFrame(frame)
            resizeObserver.disconnect()
            controls.dispose()
            canvas.removeEventListener('model-url-change', onModelUrlChange)
            disposeModel()
            renderer.dispose()
          },
          { once: true }
        )
      } catch {
        if (status) status.textContent = '3D preview unavailable'
      }
    }

    function initPlaygroundDemo() {
      const runMobile = document.getElementById('pg-run-mobile')
      if (!runMobile || runMobile.dataset.bound === '1') return
      runMobile.dataset.bound = '1'

      const $ = (id: string) => document.getElementById(id)

      // Text lands first so the block is never blank; the tokenized markup
      // replaces it once Shiki resolves. A newer paint wins on late arrivals.
      const paintJson = (el: HTMLElement, code: string) => {
        el.textContent = code
        const stamp = String(Number(el.dataset.paint ?? '0') + 1)
        el.dataset.paint = stamp
        void import('../lib/highlight').then(({ highlightInline }) => {
          const html = highlightInline(code, 'json')
          if (html && el.dataset.paint === stamp) el.innerHTML = html
        })
      }
      const modal = $('auth-modal')!
      const strip = $('signed-in-strip')!
      const meter = $('free-meter')!
      const overlay = $('running-overlay')!
      const upgrade = $('upgrade-overlay')!
      const fill = $('progress-fill')!
      const resultPreview = $('result-preview')!
      const inputJson = $('input-json')!
      const resultJson = $('result-json')!
      const modalCredits = $('modal-credits')!
      const base = Number(runMobile.dataset.baseCredits ?? '6')
      const slug = runMobile.dataset.slug ?? 'model'
      const defaultImageName = runMobile.dataset.defaultImageName ?? null
      const defaultImageUrl = runMobile.dataset.defaultImageUrl ?? null
      const defaultModelUrl = runMobile.dataset.defaultModelUrl ?? null
      const workflowUrl = runMobile.dataset.workflowUrl ?? null
      const runDesktop = $('pg-run-desktop')

      // BrandButton wraps labels in a centering span — writing to the button
      // itself would strip it.
      const label = (el: HTMLElement) => el.querySelector('span') ?? el
      const runMobileLabel = label(runMobile)
      const runDesktopLabel = runDesktop ? label(runDesktop) : null

      let authed = false
      let runsLeft = 5
      let running = false
      let refImage: Pixal3dImage | null =
        defaultImageName && defaultImageUrl
          ? { name: defaultImageName, source: defaultImageUrl }
          : null
      let lastRun: Record<string, unknown> | null = null

      const renderInputJson = () => {
        const payload: Record<string, unknown> = {
          workflow: slug,
          inputs: { image: refImage?.name ?? null, output_format: 'glb' },
          estimate: { credits: base, charged_on: 'success only' }
        }
        paintJson(inputJson, JSON.stringify(payload, null, 2))
      }

      const renderResultJson = () => {
        const receipt = lastRun ?? {
          status: 'example',
          note: 'This is a curated example. Run it to get your own receipt.',
          model: slug,
          output: { type: 'model/gltf-binary', format: 'glb' }
        }
        paintJson(resultJson, JSON.stringify(receipt, null, 2))
      }

      const setRunLabels = () => {
        modalCredits.textContent = String(base)
        const text = workflowUrl
          ? 'TRY WORKFLOW'
          : running
            ? 'Running…'
            : !authed
              ? 'Sign in to run — 5 free'
              : runsLeft > 0
                ? `Run — ${runsLeft} free left`
                : 'Go Creator — from $20/mo'
        runMobileLabel.textContent = text
        if (runDesktopLabel) runDesktopLabel.textContent = text
      }

      const setRunning = (value: boolean) => {
        running = value
        runMobile.toggleAttribute('disabled', value)
        runDesktop?.toggleAttribute('disabled', value)
        setRunLabels()
      }

      const renderAll = () => {
        renderInputJson()
        renderResultJson()
        setRunLabels()
      }

      // result view toggle (Preview | JSON)
      $('rview-toggle')!.addEventListener('click', (e) => {
        const btn = (e.target as HTMLElement).closest<HTMLElement>(
          '[data-rview]'
        )
        if (!btn) return
        const json = btn.dataset.rview === 'json'
        $('result-json-box')!.classList.toggle('hidden', !json)
        resultPreview.classList.toggle('hidden', json)
        $('result-meta')!.classList.toggle('hidden', json)
        $('result-download')!.classList.toggle('hidden', json)
        for (const b of $('rview-toggle')!.querySelectorAll<HTMLElement>(
          '[data-rview]'
        )) {
          const active = b === btn
          b.classList.toggle('bg-transparency-white-t8', active)
          b.classList.toggle('text-primary-comfy-canvas', active)
          b.classList.toggle('text-primary-warm-gray', !active)
          b.classList.toggle('hover:bg-transparency-white-t4', !active)
          b.classList.toggle('hover:text-primary-comfy-canvas', !active)
        }
        renderResultJson()
      })

      // reference image demo
      const exampleButtons = [
        ...document.querySelectorAll<HTMLElement>('[data-example-image]')
      ]

      const setExampleSelection = (name: string | null) => {
        for (const button of exampleButtons) {
          const selected = button.dataset.exampleName === name
          if (selected) button.dataset.selected = ''
          else delete button.dataset.selected
          button.setAttribute('aria-pressed', String(selected))
        }
      }

      const selectExample = (
        name: string,
        url: string,
        modelUrl?: string | null
      ) => {
        refImage = { name, source: url }
        $('image-error')?.classList.add('hidden')
        setExampleSelection(name)
        if (modelUrl) {
          $('result-model-canvas')?.dispatchEvent(
            new CustomEvent('model-url-change', { detail: { url: modelUrl } })
          )
          const download = $(
            'result-model-download'
          ) as HTMLAnchorElement | null
          if (download) download.href = modelUrl
          const outputName = $('result-output-name')
          if (outputName)
            outputName.textContent = modelUrl.split('/').at(-1) ?? name
        }
        renderAll()
      }

      for (const button of exampleButtons) {
        button.addEventListener('click', () => {
          const exampleName = button.dataset.exampleName
          const exampleUrl = button.dataset.exampleUrl
          if (!exampleName || !exampleUrl) return
          selectExample(exampleName, exampleUrl, button.dataset.exampleModelUrl)
        })
      }

      let progressAnimation: Animation | null = null
      function stopProgressAnimation() {
        progressAnimation?.cancel()
        progressAnimation = null
        fill.style.transform = ''
      }

      const startRun = () => {
        if (running) return
        if (runsLeft <= 0) {
          upgrade.classList.remove('hidden')
          upgrade.classList.add('flex')
          return
        }
        setRunning(true)
        strip.classList.remove('hidden')
        strip.classList.add('flex')
        overlay.classList.remove('hidden')
        overlay.classList.add('flex')
        $('result-json-box')!.classList.add('hidden')
        resultPreview.classList.remove('hidden')
        $('result-meta')!.classList.remove('hidden')
        $('result-download')!.classList.remove('hidden')
        stopProgressAnimation()
        fill.style.width = '0%'
        $('run-error')?.classList.add('hidden')
        requestAnimationFrame(() =>
          requestAnimationFrame(() => (fill.style.width = '92%'))
        )
        window.setTimeout(() => {
          overlay.classList.add('hidden')
          overlay.classList.remove('flex')
          resultPreview.style.opacity = '0.4'
          window.setTimeout(() => (resultPreview.style.opacity = '1'), 350)
          runsLeft -= 1
          lastRun = {
            job_id: 'j_pixal3d',
            status: 'succeeded',
            model: slug,
            credits_charged: base,
            charged_on: 'success only',
            duration_s: 18.6,
            nodes_executed: 66,
            output: {
              type: 'model/gltf-binary',
              format: 'glb',
              saved_to_history: true
            }
          }
          meter.textContent =
            runsLeft > 0 ? `Free runs: ${runsLeft} left` : 'Free runs used'
          renderAll()
          setRunning(false)
          if (runsLeft <= 0) {
            window.setTimeout(() => {
              upgrade.classList.remove('hidden')
              upgrade.classList.add('flex')
            }, 900)
          }
        }, 2400)
      }

      const onRun = () => {
        if (workflowUrl) {
          window.open(workflowUrl, '_blank', 'noopener,noreferrer')
          return
        }
        if (!refImage) {
          $('image-error')?.classList.remove('hidden')
          return
        }
        if (!authed) {
          modal.classList.remove('hidden')
          modal.classList.add('flex')
        } else startRun()
      }
      runMobile.addEventListener('click', onRun)
      runDesktop?.addEventListener('click', onRun)
      $('free-run-cta')?.addEventListener('click', onRun)

      modal.querySelectorAll('[data-auth]').forEach((button) =>
        button.addEventListener('click', () => {
          modal.classList.add('hidden')
          modal.classList.remove('flex')
          authed = true
          setRunLabels()
          startRun()
        })
      )
      $('auth-close')!.addEventListener('click', () => {
        modal.classList.add('hidden')
        modal.classList.remove('flex')
      })
      $('demo-reset')!.addEventListener('click', () => {
        runsLeft = 5
        lastRun = null
        if (defaultImageName && defaultImageUrl) {
          selectExample(defaultImageName, defaultImageUrl, defaultModelUrl)
        }
        upgrade.classList.add('hidden')
        upgrade.classList.remove('flex')
        strip.classList.add('hidden')
        strip.classList.remove('flex')
        meter.textContent = 'Free runs: 5 left'
        renderAll()
      })

      renderAll()
    }

    const initAll = () => {
      initModelTabs()
      initSdkTabs()
      initCodeCopy()
      void initModelViewer()
      initPlaygroundDemo()
    }

    initAll()
    document.addEventListener('astro:page-load', initAll)
  </script></BaseLayout>
