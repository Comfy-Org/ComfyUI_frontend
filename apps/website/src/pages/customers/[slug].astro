---
import type { GetStaticPaths } from 'astro'
import BaseLayout from '../../layouts/BaseLayout.astro'

export const getStaticPaths = (() => {
  return [
    // TODO: Add customer story entries
    // { params: { slug: 'story-name' }, props: { title: 'Story Title' } },
  ]
}) satisfies GetStaticPaths

const { title = '' } = Astro.props
---

<BaseLayout title={`${title} — Customer Stories`}>
  <!-- TODO: Add customer story content -->
</BaseLayout>
