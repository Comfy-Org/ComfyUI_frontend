/**
 * A card marks what it is on the left and who made it on the right. Both
 * corners share one shape so a mixed grid reads evenly.
 */
export const cardBadgeClass =
  'absolute top-4 z-10 inline-flex h-10 min-w-10 items-center justify-center gap-1 rounded-2xl bg-black/40 text-white backdrop-blur-md'
