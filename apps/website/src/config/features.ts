/**
 * Marketing website feature flags. Flip a value here to toggle a feature
 * site-wide. Keep this file small — it is imported by page components.
 */

/** Show the Free tier on /cloud/pricing. Flip to `true` to re-enable. */
export const SHOW_FREE_TIER = false
