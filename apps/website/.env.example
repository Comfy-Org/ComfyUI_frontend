# Optional. Overrides the committed public CMS default (DEFAULT_CMS_URL); the
# build works without it. Set this to point at a local CMS for dev — start the
# CMS first: see apps/cms/README.md
WEBSITE_CMS_URL=http://localhost:3000
