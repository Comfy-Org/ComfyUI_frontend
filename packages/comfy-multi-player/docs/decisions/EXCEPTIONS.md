# Invariant exceptions

Deliberate deviations from `docs/INVARIANTS.md` require explicit maintainer review before implementation. Add one row per approved deviation; do not use this log to retroactively excuse a violation. Rows marked **PROPOSED** were written by a reviewer and are NOT yet agreed: their owner and sunset date are the maintainer's to assign, and until they are, the row records the deviation without claiming it was signed off.

| Date | Invariant ID | Why | Scope | Expiry |
| --- | --- | --- | --- | --- |
| YYYY-MM-DD | KA-0 / FC-0 | Decision and approver | Exact files, release, or deployment | Date or removal condition |
| ~~2026-08-21 (PROPOSED)~~ **RESOLVED 2026-08-21** | KA-4 | ~~Four `connect` rejections mutate before validating (issue #10).~~ **Sunset condition met: PR #34 landed.** The block asserting the bug went red exactly as designed, the four rows moved into `CASES` in `test/ka4-rejection-byte-identity.test.ts`, and `KNOWN_KA4_VIOLATIONS` is now empty. Struck rather than deleted so the exception's full life stays legible. | (was `src/applier.ts` `connect` paths) | Expired. Amendment A9 closes the cloneable-but-unstorable, `link_id`, and `removed_links` write-order holes; Amendment A10 closes cycles; Amendment A14 closes the `link_type` shape hole before any write. |
| 2026-08-21 (PROPOSED) | KA-4 | **Permanent convergence carve-outs**, schema §2.5 items 4-6 and 8 / Amendment A6, and the RULE that generates them: a rejection is arrival-order-dependent exactly when the check raising it READS THE DOCUMENT and sits below an early return that consumes the `op_id` (a delete-wins return or the interior null return). Under §4 abort-remainder such a rejection also decides whether the rest of the batch applies, which turns a bookkeeping difference into a projection difference. Item 7's `add_node` exception was resolved by Amendment A7's node-presence stamp gate and its pin flipped positive. Every OP-ONLY precondition is hoisted above those returns and does NOT carve out. Measured in both orders with converging controls. | `src/applier.ts` `applyConnect` / `applySetWidget` / `resolveInteriorNode`; schema §2.5 items 4-6 and 8 | **No sunset — deliberate.** A6 rejects three alternatives with reasons; closing any remaining item either re-opens issue #10 or changes the concrete-input register's semantics, which is a vocabulary change needing a comfy-cli counterpart. Remaining items are pinned by still-diverges tests. Revisit only via a new amendment. |
| 2026-08-21 (PROPOSED; scope widened 2026-08-27 by schema Amendment A15 — a NESTED promoted host write resolves its `instance_path` through the same guard and is rejected the same way) | KA-3 | This applier REJECTS an interior write into a shared subgraph definition where comfy-cli FORKS it, and it counts instances by resolved definition where comfy-cli's `engine._count_instances` matches the literal id string. Pre-declared by schema §5.3, which keeps forking OPEN; recorded here so the divergence has an owner and a sunset rather than living only in source comments. | `src/doc.ts` `countDefinitionInstances`/`definitionAliases`; `src/applier.ts` §5.3 guard | Owner: UNASSIGNED. Sunset PROPOSED, not agreed: the schema §5.3 forking amendment lands and both implementations adopt it. |
| 2026-08-21 (PROPOSED) | KA-11 | **The snapshot read surface's fail-closed gate is CONDITIONAL**: `src/read.ts` refuses a document carrying content under an unreadable schema, but returns its empty value for a document carrying nothing, where `project()` refuses that document too. Deliberate — ADR-004's follower reads a root-less document before its first frame and there is no content there to mis-key — but it is a read path that answers where a sibling read path throws, so it is recorded rather than left in source comments. Normative text: Amendment A12. The divergence is disposition-only; both paths use `readSchemaVersion` as the predicate. Mitigated by asking "carries content" against the struct store rather than the v1 root names, so a v2 that renamed its roots is refused rather than read as empty. | `src/read.ts` `assertSnapshotReadable`/`carriesContent`; schema §10 + Amendment A12 | Owner: UNASSIGNED. Sunset PROPOSED, not agreed: revisit when `SCHEMA_VERSION` first moves past 1, since a real v2 makes the disposition split observable to a deployed follower for the first time. |
| ~~2026-08-27 (PROPOSED)~~ **RESOLVED 2026-08-28** | KA-3 | ~~**A promoted `connect` (`grow.promoted: true`, schema Amendment A15) is LWW-gated here on `("input", to_node, "grow", name)` and moves the materialized slot's `grow_id` to the winning link; comfy-cli's `_apply_connect` gates neither.**~~ **Sunset condition met: comfy-cli PR #818 (amendment v1.5, `ba0b0b92abcc86b01e8a6704d07088f92afe7aa7`) gates the same register, spelled with the FULL declared name on both sides, and moves `grow_id` to the winner.** Struck rather than deleted so the exception's life stays legible. Both spell the register identically (`_write_target`), so the divergence is in GATING only and is observable only under concurrency comfy-cli's single-writer apply never sees — the corpus session it generated replays green. Gating is required for §2.5 convergence (two connects into one declared input contend for one slot exactly as two concrete connects do, A1). | `src/applier.ts` `claimPromotedInput`; schema Amendment A15 | Owner: UNASSIGNED. Sunset PROPOSED, not agreed: comfy-cli `_lww_gate`s promoted grows on the same register (asked for in the PR that lands A15). |
