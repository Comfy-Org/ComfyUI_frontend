--- a/ComfyUI-Custom-Scripts-HEAD/web/js/stringFunction.js
+++ b/ComfyUI-Custom-Scripts-HEAD/web/js/stringFunction.js
@@ -1,34 +1,21 @@
-import { app } from "../../../scripts/app.js";
-import { ComfyWidgets } from "../../../scripts/widgets.js";
-
 // Displays input text on a node
 
-app.registerExtension({
-	name: "pysssss.StringFunction",
-	async beforeRegisterNodeDef(nodeType, nodeData, app) {
-		if (nodeData.name === "StringFunction|pysssss") {
-			const onExecuted = nodeType.prototype.onExecuted;
-			nodeType.prototype.onExecuted = function (message) {
-				onExecuted?.apply(this, arguments);
-
-				if (this.widgets) {
-					const pos = this.widgets.findIndex((w) => w.name === "result");
-					if (pos !== -1) {
-						for (let i = pos; i < this.widgets.length; i++) {
-							this.widgets[i].onRemove?.();
-						}
-						this.widgets.length = pos;
-					}
-				}
-
-				const w = ComfyWidgets["STRING"](this, "result", ["STRING", { multiline: true }], app).widget;
-				w.inputEl.readOnly = true;
-				w.inputEl.style.opacity = 0.6;
-				w.value = message.text;
-
-				this.onResize?.(this.size);
-			};
+comfy.defs.extend("StringFunction|pysssss", (builder) => {
+	builder.onExecuted((node, result) => {
+		const names = node.widgets.names();
+		const pos = names.indexOf("result");
+		if (pos !== -1) {
+			for (const name of names.slice(pos)) {
+				node.widgets.remove(name);
+			}
 		}
-	},
+
+		node.widgets.add({
+			type: "textarea",
+			name: "result",
+			value: result.raw.text,
+			disabled: true,
+		});
+	});
 });
 
