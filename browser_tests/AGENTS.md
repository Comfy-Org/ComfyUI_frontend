# E2E Testing Guidelines

The canonical guide for browser tests is **`browser_tests/README.md`** — setup,
directory structure, conventions, typed mocks, flake prevention, and the
screenshot workflow all live there. It is imported below so it auto-loads when
working in this directory.

@browser_tests/README.md

See `@docs/guidance/playwright.md` for the `*.spec.ts` auto-load stub and
`.agents/checks/playwright-e2e.md` for the review rulebook. Both point back to
the canonical guide above.
