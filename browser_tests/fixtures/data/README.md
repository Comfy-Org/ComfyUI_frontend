# fixtures/data

Static test data used by browser tests.

Place mock API responses, workflow JSON files, node definitions, and other
static data here. Files in this directory should contain **only data** — no
executable code, no Playwright imports, no page dependencies.

For test assets like images and full workflow files used via file upload, see
`browser_tests/assets/` instead.
