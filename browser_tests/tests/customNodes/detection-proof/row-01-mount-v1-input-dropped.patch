diff --git a/src/services/litegraphService.ts b/src/services/litegraphService.ts
--- a/src/services/litegraphService.ts
+++ b/src/services/litegraphService.ts
@@ -338,4 +338,17 @@ export const useLitegraphService = () => {
     // Create sockets and widgets in the determined order
-    for (const inputSpec of orderedInputSpecs) addInputSocket(node, inputSpec)
-    for (const inputSpec of orderedInputSpecs) addInputWidget(node, inputSpec)
+    // DETECTION PROOF (row 1, mount v1): drop ImpactInt's declared value
+    // only in the canvas renderer. Expected: S1 red and S2 remains green.
+    const detectionProofTier = (
+      globalThis as {
+        __COMFY_CUSTOM_NODE_DETECTION_PROOF_TIER__?: string
+      }
+    ).__COMFY_CUSTOM_NODE_DETECTION_PROOF_TIER__
+    const detectionProof =
+      detectionProofTier === 'S1' &&
+      nodeDefImpl.name === 'ImpactInt' &&
+      !useSettingStore().get('Comfy.VueNodes.Enabled')
+        ? orderedInputSpecs.slice(0, -1)
+        : orderedInputSpecs
+    for (const inputSpec of detectionProof) addInputSocket(node, inputSpec)
+    for (const inputSpec of detectionProof) addInputWidget(node, inputSpec)
   }
