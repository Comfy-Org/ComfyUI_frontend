diff --git a/src/composables/node/useNodeBadge.ts b/src/composables/node/useNodeBadge.ts
index cfe2ad66f2..2b7f328ed9 100644
--- a/src/composables/node/useNodeBadge.ts
+++ b/src/composables/node/useNodeBadge.ts
@@ -216,8 +216,9 @@ export const useNodeBadge = () => {
         )
       },
       afterConfigureGraph() {
-        for (const node of app.canvas.graph?.nodes ?? [])
-          priceBadge.updateSubgraphCredits(node)
+        // DETECTION PROOF (row 7, load hook): extension hook throws on load.
+        // Expected: Core workflow load red in both-renderer console checks.
+        throw new Error('DETECTION PROOF: afterConfigureGraph crash')
       }
     })
   })
